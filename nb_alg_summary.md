# 各NB改进算法：配置、训练、预处理、参数易错点总结
> 全部基于WEKA论文实验，数据集为UCI；原始数据集含有大量连续数值属性。
> 涉及算法：CAVWNB、A²WNB、MAWNB、EMAWNB、PMWNB、DIWNB

## 一、离散化处理：哪些需要，哪些不需要
|算法 | 是否必须做离散化（Discretize） | 论文使用的离散化方式 | 说明 |
|---|---|---|---|
|CAVWNB | ✅必须 | 等宽10箱 EWD | 原始算法只支持**离散属性**，连续特征必须先离散化 |
|A²WNB | ✅必须 | 等宽10箱 EWD | RODE基分类器只能接收离散输入，原始连续属性必须离散化 |
|MAWNB | ✅必须 | 等宽10箱 EWD | SPODE、RandomTree基分类器要求输入离散数据 |
|EMAWNB | ✅必须 | 等宽10箱 EWD | MAWNB的增强版，基分类器同样只接受离散输入；交叉验证构造标签视图也要求离散属性 |
|PMWNB | ❗内置两套离散化，**不使用WEKA全局Discretize过滤器** | EWD（等宽）、MDLP（监督离散） | PMWNB内部自己做离散化生成两套Base View；**不要在数据集上提前全局离散**。如果提前全局离散，会破坏它需要的原始数值，导致两种扰动视图完全一样，算法失效 |
|DIWNB | ✅论文实验做了全局离散 | 等宽10箱EWD | KNN距离计算可以支持连续，但论文实验统一全部先离散化再跑；DIWNB本身逻辑可以跑连续，复现论文结果必须做离散化预处理 |

> ⚠️重点坑：
> 1. A²WNB / MAWNB / EMAWNB / CAVWNB / DIWNB：复现论文结果，数据集**先全局Discretize（Equal‑Width，10 bins）**。
> 2. PMWNB：**禁止提前全局离散！**，把原始数值数据集喂给PMWNB，算法内部会分别用EWD、MDLP生成两个基础视图；如果你先把数据集离散完，PMWNB的输入全部变成离散值，MDLP、EWD扰动不再产生差异，核心多矩阵视图机制废掉，性能直接掉一大截。

## 二、各个算法关键参数、目标函数、容易接错/配错的点
### 1. CAVWNB（类‑属性值加权朴素贝叶斯）
1. 目标函数：最小化**负条件对数似然(-CLL)**，带L2正则化。
2. 优化器：L‑BFGS‑M；**正则化λ默认=1.0**，这个参数不能设0，容易权重爆炸过拟合。
3. 权重粒度：**类 × 属性值**，每个类的每一个属性值拥有独立权重，不是属性整体权重。
4. ❗易错：
    - 不要错用成最大化CLL；目标是最小化负条件对数似然。
    - 不能把λ直接置0，会严重过拟合。
    - 输入必须离散，连续直接跑会报错。

### 2. A²WNB（Attribute augmented and weighted NB）
1. 核心模块：
    - 第一阶段：训练一批**RODE（Random one‑dependence estimators）**，用RODE预测输出标签，作为**新增潜在属性latent attributes**，拼接到原始属性后面，得到增广属性集。
    - 第二阶段：WANBIA‑CLL，对**拼接后的全部增广属性（原始+潜在标签属性）**做属性权重学习，L‑BFGS‑M优化，最大化条件对数似然CLL。
2. 参数：潜在属性数量s，论文默认 `s=m`（m为原始属性个数）；推荐范围 `s ≥ m/2`，性能不敏感，但s太小效果明显下降。
3. ❗易错坑点：
    - 不要只给潜在属性加权，**原始属性也要一起参与权重优化**。
    - RODE的父属性选择逻辑：找条件互信息最大的 `log₂m` 个候选，再随机选一个作为父节点，这个随机逻辑不能删掉；如果固定父节点，就不再是RODE。
    - 输入必须离散化，RODE不支持连续值。

### 3. MAWNB（Multi‑view Attribute Weighted Naive Bayes）
1. 多视图构成：
    - 原始属性视图View‑1；
    - SPODE输出预测标签生成标签视图View‑2；
    - RandomTree输出预测标签生成标签视图View‑3。
2. 每个视图独立训练一套**CAVWNB式类‑属性值加权模型**，L‑BFGS‑M、L2正则λ=1.0。
3. 融合方式：**直接等权平均各个视图输出的后验概率**，没有学习视图权重。
4. ❗易错：
    - 基分类器数量：m个SPODE、m个RandomTree（m=属性数目），不要写成固定常数。
    - MAWNB没有交叉验证：**训练集既用来训练基分类器，又用来生成标签视图，存在标签泄露，会过拟合**，这也是后面EMAWNB要解决的问题。
    - 全部输入必须离散。

### 4. EMAWNB（Enhanced‑MAWNB）两个版本 Eager / Lazy
解决MAWNB两大缺陷：标签泄露过拟合 + 视图等权融合不合理。
1. 参数：
    - 交叉验证折数 `s=10`（默认）；用k折拆分，用fold外样本生成标签视图，消除标签泄露。
    - Lazy版本邻域大小 `k=30`（默认）。
2. 两个融合策略：
    - EMAWNB‑E(Eager)：**全局训练集学习一套固定视图权重**；
    - EMAWNB‑L(Lazy)：**每一条测试样本，基于它k近邻动态算出视图权重**。
3. 每个视图内部，依然沿用和CAVWNB一致的：负CLL损失 + L2‑λ=1.0 + L‑BFGS‑M优化。
4. ❗易错：
    - 不要把s=1直接设置，s过小，交叉验证缓解泄露的效果很差。
    - 不要直接照搬MAWNB简单平均概率，EMAWNB必须使用**学到的视图权重加权融合**。
    - Lazy版本k=30，不要设置太小，权重估计会抖动。
    - 输入全部要求离散。

### 5. PMWNB（Perturbation‑driven Matrix‑view Weighted NB）
> ⚠️重点：内部自带离散，**禁止数据集提前全局Discretize**。
1. 扰动生成两个基础Base View：
    - View‑V11：EWD等宽离散（K_EWD=10）；
    - View‑V21：MDLP监督离散。
2. 在每个base view上，分别训练m个SPODE、m个RT；
    分别产出：标签视图（hard label输出）、概率视图（soft后验概率输出），一共4个潜在视图，构成矩阵视图。
3. 每个视图内部做类‑属性值加权；概率视图还要再做一次离散对齐；L‑BFGS‑M，λ=1.0。
4. 融合：把矩阵全部视图的后验概率**直接取平均**输出预测类别。
5. 扩展版本PMWNB‑Ext：增加更多离散策略，精度提升有限，训练时间大幅上涨，论文默认不用。
6. ❗易错：
    - 千万不要提前对数据集做全局离散，会让EWD、MDLP输入完全一样，扰动失效。
    - soft概率视图**必须二次离散**，不然加权模块不能接收连续概率值。

### 6. DIWNB（Dual‑view Instance Weighted NB）
分为 $\text{DIWNB}_E^H$、$\text{DIWNB}_L^H$（hard标签版本）、$\text{DIWNB}^S$（soft概率版本）。
1. 生成Generated View：使用一组不同k值KNN基分类器，选误差最低的β个KNN；
    - hard版本：把KNN预测标签当新增属性；
    - soft版本：把KNN输出的概率拼接作为新增属性。
2. 参数：α=15（KNN候选数量），β=5（筛选保留最好的KNN数量）。
3. 两个视图（原始视图 + 生成视图）分别训练实例加权朴素贝叶斯；
4. 融合：基于各视图在训练集的误差，学习视图权重做加权融合。
5. ❗易错：
    - 论文实验做了全局离散，但算法原生支持连续特征（KNN可算距离）。**复现论文结果就离散；自己跑原始连续数据集可以不做离散。**
    - β不要设置过大，会引入大量质量差的KNN。

## 三、数据集本身：数值型 / 已离散，哪些算法处理会出错
1. **原始UCI数据集（大量数值属性，未离散）**
    - ✅PMWNB：直接喂原始数值数据集。内部自己执行EWD、MDLP两套离散。
    - ✅DIWNB：原生支持连续。论文为统一对比，实验时**额外加Discretize过滤器**变成离散。
    - ❌CAVWNB / A²WNB / MAWNB / EMAWNB：**直接喂原始数值数据集会出错**，基分类器不支持连续，必须前置全局10箱等宽离散。

2. **已经提前全部离散好的数据集**
    - ❌PMWNB：**绝对不能用**。输入全部变成离散值，EWD和MDLP扰动不再产生差异，两个base view完全一致，矩阵视图的多样性被破坏，算法核心失效，性能大幅下降。
    - ✅CAVWNB / A²WNB / MAWNB / EMAWNB：可以直接跑，不需要再离散。
    - ✅DIWNB：可以直接跑。

## 四、复现论文的最简预处理流程总结
1. 数据集统一处理：缺失值填充（数值取均值，标称取众数）。
2. 分两套：
    - ①跑CAVWNB / A²WNB / MAWNB / EMAWNB / DIWNB：加过滤器`Discretize`等宽，10个bin。
    - ②跑PMWNB：**不要加Discretize过滤器，保留原始数值属性，直接送入PMWNB算法**。
3. 评估统一：**分层10‑fold交叉验证，重复运行10次取平均**。

> 高频踩坑复盘：
> - 很多人复现PMWNB，照搬其他算法，数据集先全局离散，PMWNB效果很差，就是这个原因。
> - EMAWNB不要简化去掉k折交叉验证，退回MAWNB，就会复现出过拟合，测试集精度下降。
> - 所有加权模型（CAVWNB、MAWNB、EMAWNB、PMWNB）λ正则不要设0，容易权重爆炸。
