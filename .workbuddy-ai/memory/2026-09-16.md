# 2026-09-16

## 排查：homepage-demo 是否存在代码引用（结论：零依赖，可安全删除）

排查范围：全项目（backend / frontend / scripts / docs / 启动脚本 / docker / 根级 md）。

结论 —— **没有任何代码实际使用 `homepage-demo/`**：

1. 全项目唯一提到 `homepage-demo` 字样的地方是它自己内部的 `homepage-demo/字段来源说明.md:3`（自引用）。
2. 后端静态挂载不涉及它：`backend/app/main.py:1997` 挂的是 `SITE_DIR`；
   `main.py:659-669` 定义 `WEB_DIR = frontend/`、`DIST_DIR = frontend/dist`、
   `SITE_DIR = DIST_DIR if (DIST_DIR/"index.html").exists() else WEB_DIR`
   → 当前 dist/index.html 存在，实际服务的是 `frontend/dist`（Vue 构建产物）。
3. `start_all.bat` / `start_all.ps1` / `docker-compose.yml` / `scripts/` / 根级 `README.md` / `nb_alg_summary.md` / `docs/*.md` 均无引用。
4. `frontend/public/`、`frontend/dist/` 里没有 demo 的任何 HTML 副本。
5. demo 自身是纯自包含静态原型：所有 href 均为内部相对路径，无 `../` 外部引用，无 fetch/axios/API/localhost 调用。

功能已被 Vue 组件完整取代（9 个 demo ↔ 前端组件一一对应）：
- 管理端 5 个（admin/overview + network/power/flightdeck/geological）
  → `frontend/src/views/Home/dashboard/AdminOverviewPage.vue` + `AdminProfilePage.vue` + `sections/Profile{Network,Power,Flightdeck,Geological}.vue`
- 用户端 4 个（user/network/power/flightdeck/geological）
  → `UserWorkspacePage.vue` + `sections/Workspace{Network,Power,Flightdeck,Geological}.vue`
- 分发入口：`DashboardHomeView.vue`

## 删除前需注意

- `homepage-demo/` 是 **git 已跟踪** 目录，`git status` 显示 `admin/*.html` 5 个文件有未提交修改 —— 删除会产生 git 变更，需先确认这些改动是否还需要。
- 目录内含 `首页.zip`（打包交付物）与 `backup-current-5/`（切 9 宫格前的老 5 个首页备份），属历史存档。

## 改造：settings-demo/index.html 三处 UI 问题（已用 Playwright 截图验证）

> 背景：`settings-demo/index.html` 是**孤立的静态原型**（除自身外全项目无引用），但与 `frontend/src/views/Model/Settings.vue` 一一对应，是设置页的设计稿。用户会拿它当视觉验收对象，改动需同步考虑 app 侧。

用户给了 3 张截图，**确认全部来自 `settings-demo/index.html`**（判据：demo 的场景开关漏了 knob、`status-badge--on` 只在 demo/UserManagement 有定义；真实 app `Settings.vue` 的开关都有 knob）。

改动：
1. **卡片被强行等高** → `.tabpanel` 加 `align-items:start`，卡片改为「内容多长就多长」。
2. **启用徽标绿色太浅** → `.status-badge--on` 改为深绿底 `rgba(14,99,76,.62)` + 亮绿字 `#6ef0c4` + 描边；`--off` 同步加深。
3. **AI 卡片重构** → 只读展示 接口地址/模型名称/API key（`detail-info` 行，未配置时显示「未填入」），「保存 AI 设置」改为「编辑」按钮 + `#aiDialog` 弹窗；key 掩码保留前 3 后 4（`sk-••••••••3f9c`）；弹窗 key 留空 = 保留原 key，首次配置留空则报错。
4. **场景开关缺 knob**（渲染成光秃秃的蓝色药丸）→ 给 4 个 `settings-switches__toggle` 补上 `<span class="settings-switches__knob">`。

## 可复用的浏览器验证流程（本机）

- `agent-browser` 未安装；改用已缓存的 Chromium：`npm install @playwright/cli@latest` 装到 `C:\Users\11543\.workbuddy-ai\binaries\node\workspace`。
- **关键坑**：`playwright-cli` 的浏览器会话**不跨 Bash 调用存活**，必须把 open→操作→screenshot→close 写在**同一条命令**里（用 `;` 串联）。
- **关键坑**：`file://` 协议被禁用，必须起本地 HTTP 服务：`python -m http.server 8777 --bind 127.0.0.1`（后台跑）。
- 在 cwd 放 `playwright-cli.json` 内容 `{"outputMode":"stdout"}` 可让 snapshot 直接打到 stdout。

## 同步到真实 app（`frontend/src/views/Model/Settings.vue`）

`vue-tsc --noEmit -p tsconfig.app.json` 通过（无报错）。

1. `.settings-grid` 加 `align-items: start` —— 管理员视图「场景启停 / 自动刷新」两卡不再被拉伸。
2. AI 卡片重构：删掉 `aiForm`（内联表单）与 `.ai-configured`，改为
   - 只读行 `aiReadonlyRows`（computed，取 `aiSetting.base_url / model / api_key_masked`，空值显示「未填入」）+ 启用状态行
   - 徽标 `.ai-badge` + `--on/--off`（沿用 demo 的深绿/深红）
   - 按钮「保存 AI 设置」→「编辑」，弹 `#aiDialog`（`el-dialog`），保存逻辑走 `aiEditForm`：key 留空 = 保留服务端已保存的 key，首次配置留空则拦截
3. **关键坑（务必记住）**：本项目所有弹窗都用 `append-to-body`，而 `.settings-form__*` / `.settings-btn` / `.settings-switches__*` 全在 **scoped** 块里 —— scoped 样式**到不了 teleport 出去的弹窗内容**。所以新弹窗的样式必须写进**第二个非 scoped 的 `<style>` 块**（该文件 1026 行起，已有 `.threshold-log-dialog` 先例），且页脚按钮改用全局已适配暗色的 `el-button`。我把开关样式在全局块里按 `.ai-setting-dialog .settings-switches__*` 复制了一份。
4. 顺手清理：删除已无引用的 `.settings-form--ai`（含媒体查询里的一处）。
5. `UserManagement.vue` 的 `.status-badge--on/--off` 同步换成深色版（与 demo 保持一致，原为同一套淡色）。

## 视觉验证（已做，用项目自带脚本 + Playwright）

**前提发现：整套环境当时已在运行**（Postgres:5432、FastAPI:12312、Vite:5173、Java PMWNB:12313），**不需要**跑 `start_all.ps1`。

> ⚠️ **不要跑 `start_all.ps1`**：第 23 行 `Get-Process -Name java,node,python | Stop-Process -Force` 会**杀掉本机所有** java/node/python 进程（不只它自己起的），且结尾是 `while($true)` 死循环。

**登录凭据（重要）**：`admin` / **`123456`**。注意 `scripts/seed_test_data.py` 的 `TEST_USERS` 写的是 `admin123`，**与实际库不符**（`verify_page_render.py` 的 help 里已备注"实测种子密码是 123456"）。其余：net_admin/net123456、alice/alice123、bob/bob123456、carol/carol123456。

**验证方式**：
- 项目自带 `scripts/verify_page_render.py`（无头 Chrome + CDP，先 fetch 登录 → reload → `location.hash` 跳转 → present/absent 断言 + 截图）。用隔离 venv 的 python 跑。
- Vite dev server（5173）会把 `/api` 代理到 12312，且直接从 `src/` 编译，**改 src 立即生效，不用 build**（后端 12312 托管的是 `frontend/dist`，是旧产物）。
- 断言结果 **PASS**：`保存 AI 设置` 已消失，`接口地址/模型名称/API key/编辑/测试连通性/启用 AI 解释` 均出现。
- 实测数值：`.settings-grid` computed `align-items = start`，两卡高度 **287 vs 220**（修复前会相等）；`.ai-readonly` 4 行，key 显示 `sk-0...LytU`。
- 弹窗实测：`inBody=true`（确认 teleport 到 body）、body padding `20px 24px`、input padding `10px 14px`、3 个输入框值为 `["https://wolfai.top/v1","gpt-5.6-luna",""]`（key 留空 = 保持）、knob 20px/50%、footer `[取消, 保存]`。

## ⚠️ 项目级隐患：style.css 的 `.el-button` 暗色覆盖是**失效**的

`style.css:1231-1251` 写的 `.el-button { background: rgba(91,166,255,.1) }` 等规则**实际不生效** —— 与 Element Plus 自身样式**同权重**，而 EP 的样式表在 style.css **之后**加载，后写者胜。

实测证据：新弹窗里 `<el-button>取消</el-button>` 渲染成 **白底 `rgb(255,255,255)` + 深灰字**，`--el-button-bg-color` 仍是 EP 默认 `#fff`；primary 也是 EP 默认 `#409eff`。

**唯一有效写法**（`ReportCenter.vue` 就是这么绕过去的）：覆盖 EP 的 CSS 变量并加 `!important`：
```css
.xxx .el-button {
  --el-button-bg-color: rgba(91,166,255,.1) !important;
  --el-button-border-color: rgba(125,201,255,.3) !important;
  --el-button-text-color: #9ad6ff !important;
}
```
或直接对属性加 `!important`。我在 `.ai-setting-dialog .el-button` 里按后者处理。

**未修 style.css 本身**（影响全站 el-button，超出本次范围）。如果后续发现别的页面按钮也是白底，根因就在这里。

## 顺带发现

`tmp/` 目录下有本次验证截图（`settings_admin.png`、`ai-dialog` 相关）。

## 梳理：报告中心（ReportCenter）完整逻辑

全链路已通读：`views/Model/ReportCenter.vue`(1299行) → `api/reportApi.ts` → `endpoints/report_routes.py` → `services/report_service.py` → `services/report_nl.py` → `report` 表。

- 端点：GET `/reports`（列表）、GET `/reports/{id}`（详情）、POST `/reports`（手填 content，**前端未接入**）、POST `/reports/generate`（服务端自动组装，前端唯一使用）、PUT `/reports/{id}/schedule`、DELETE `/reports/{id}`。
- 三级权限隔离：SUPER_ADMIN 全平台/指定用户；SCENARIO_ADMIN 强制本人场景；SCENARIO_USER 在 generate 中 scope 被硬置 `self`。
- generate 五步：权限归一化 → `_gather_events`/`_gather_records` → `_build_report_data`（九段：report_info / overview / prediction / model_analysis / model_evaluations / feature_analysis / trend / key_events / data_notes）→ `report_nl` 产出 analysis_nl + guidance_nl → `_render_content` 渲染 Markdown 存 `content`，结构化数据存 `report_data`(JSONB)。
- NL 为纯规则模板 + 可选 vLLM 润色（失败一律回退模板），不依赖 AI 可用性。
- 隔离口径（`_gather_events` / `_gather_records` 两套**不同**过滤链，务必区分）：
  - **风险事件 RiskEvent**：`scenario_id` + 角色分支（超管：`JOIN dataset ON risk_event.dataset_id` 且 `dataset.visibility='platform'`；场景用户：`created_by_user_id=本人`；场景管理员：无额外过滤）+ `scope='user'` 时叠加 `created_by_user_id=目标用户`。
  - **推理记录 InferenceRecord ⋈ ModelVersion**：`model_version.scenario_id` + 角色分支（超管：`JOIN dataset ON model_version.dataset_id` 且 `visibility='platform'`；场景用户：`user_id=本人` **且** `model_version.status='PUBLISHED'`；场景管理员：无额外过滤）+ `scope='user'` 时叠加 `user_id=目标用户`。
- 三个反直觉点：① 超管被 `platform` 可见性卡住，**场景管理员反而能看本场景全量**（含 company/personal 数据集、含非 PUBLISHED 模型）；② 事件按"创建人"过滤、记录按"执行人"过滤，且记录多一层模型状态过滤；③ 超管的"全平台"在 UI 上**强制选一个场景**，实际是"单场景的全平台可见数据"，跨场景需直接调接口传 `scenario_id=null`。
- 趋势判断需 `total >= 4`，前后半段风险占比差 > 0.05 才判升/降。

已确认缺口（仅记录，未改动）：
1. `scheduled` / `interval_days` 只存配置，**无调度器执行**（代码注释明示"暂不执行调度"）。
2. `format` 存 markdown/html/pdf，但无 html/pdf 生成；前端下载恒导出 `.md`。
3. 前端"重新生成"新建一条、不删旧条 → 会产生重复报告。
4. 场景管理员在生成弹窗可选"全平台数据"，后端强制改写为本人场景 → 前端文案误导。
5. `stores/reportStore.ts` 页面未使用（页面直连 reportApi）；`services/mockApi.ts` 仍残留报告 mock。
6. 路由 `/reports` 无角色守卫，鉴权完全靠后端。

## 改造：settings-demo/index.html 对齐正式版（顶部 bar + 左右两栏设置卡片）

`settings-demo/index.html`（自包含静态原型，全项目无引用）按用户给定的两张截图改造：

1. **上半部分 = 正式版**：新增 `App.vue` 的 `.topbar`——第一行品牌 eyebrow「AI Security Operations Center」+（非系统管理员时）右侧当前场景大字，第二行 `.nav-tabs` 导航胶囊 + 右侧头像悬停弹层。
   - 样式逐条抄自 `frontend/src/style.css:66-146`（`.topbar` / `.topbar__actions` / `.nav-tabs` / `.ghost-button`）与 `App.vue` scoped（头像、`.topbar__user-pop`）。
   - 导航项与角色可见性照抄 `App.vue` 的 `navItems`：`首页` 仅 SUPER_ADMIN；`场景中心` 非超管显示为「首页」；`设置` 超管显示为「系统设置」；`AI模型训练`/`用户管理` 仅 MGMT_ROLES。
   - 头像 = `display_name` 首字母（admin / net_admin / user01 → a / n / u），角色文案用 App.vue 的「系统管理员 / 场景管理员 / 场景用户」。
   - 页面头改用 `Settings.vue:239-242` 的正式文案（管理员「System Settings / 系统设置 / 风险阈值按当前账号和场景配置…」；非管理员「My Settings / 设置 / 选择你感兴趣的场景（可多选）…」），删掉 demo 自造的 `.settings-page__legend`「按优先级排序」行。
   - demo 专用的「预览角色」胶囊从页头移进**头像悬停弹层**，保证顶部视觉与正式版一致。

2. **下半部分 = 左右两栏**：`.tabpanel.is-active` 由单列改为 `grid-template-columns:repeat(2,minmax(0,1fr))`（对应 `Settings.vue` 的 `.settings-grid`），新增 `.card--span{grid-column:1/-1}`。
   - 账号与安全：`[账号信息 | 安全设置]`；基础设置：`[自动刷新设置 | 模型解释服务]`；风险阈值：单卡 `--span`（阈值长条需要横向空间）；场景管理：单卡占左栏（与截图一致，右栏留空）。
   - `.detail-info` 由两列改为单列左右行（label 左 / 值右 + 细分隔线），`.settings-form--2` 与 `.demo-switch*` 样式删除。
   - 场景启停行去掉 `network_security · ACTUAL` 这类原始字段名/常量（符合项目「界面不露开发用语」口径），信息移入 `title`；卡片级 hint 保留。

3. **验证方式（可复用）**：用 managed node + workspace 里的 playwright 无头截图。
   `NODE_PATH=C:/Users/11543/.workbuddy-ai/binaries/node/workspace/node_modules <managed node> script.js`，
   浏览器已在 `C:/Users/11543/AppData/Local/ms-playwright`。
   注意：demo 角色按钮在 `visibility:hidden` 的悬停弹层内，playwright 必须先 `hover('.topbar__user-avatar')` 才能 click。
   实测无 pageerror；1400px 宽下各页签渲染正常，760px 断点自动降为单列。

## 改造：settings-demo 设置页 → 正式版 Settings.vue（页签 + 两栏 + 按钮统一蓝 + 删提示文本）

**git 存档**：`8cc3237 chore: 存档（应用 settings-demo 设置页到正式版前）` —— 沿用本项目「动大改前先 commit 一份全量快照」的既有习惯（上一次是 `cd52acd work前`）。

### 正式版 `frontend/src/views/Model/Settings.vue` 重写
- 结构改为**横向页签**（`.settings-tabs` / `.settings-tab`，激活态底部蓝色渐变下划线）+ 每个页签内容为**左右两栏 grid**（`.settings-panel.is-active{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));align-items:start}`）。
- 页签：账号与安全 / 基础设置 / 风险阈值（`canConfigureThresholds`）/ 场景管理（`isSuperAdmin`）；`watch(tabs)` 在当前页签被角色隐藏时自动落到第一个可见页签。
- 新增「账号信息」卡片（用户名/角色/绑定场景/账号状态），数据全部来自 `userStore.currentUser`。徽标 `.role-badge--super/--admin/--user`、`.status-badge--on/--off`、`.scenario-tag` 从 `UserManagement.vue` 复制 —— 这些类**不是全局**的，style.css 里没有定义。
- 改密由「内联三列表单」改为**按钮 + `el-dialog` 弹窗**（原来只有非管理员有改密卡片，现在所有角色都有）；接口仍走 `userStore.changePassword`。
- AI 卡片「启用 AI 解释」由只读文字行改为**卡片上的开关**（`toggleAI` → `updateAISetting({provider,base_url,model,enabled})`，不传 api_key = 保持原 key）；编辑弹窗只留 接口地址/模型名称/API key。
- 保留全部原有逻辑：阈值校验 + 审计日志、自动刷新、场景启停。

### 用户明确要求的三件事
1. **深色按钮 → 蓝色**。截图取色确认：「测试连通性」「保存个人设置」都是 `#0d1d30`（即旧 `.settings-btn` 的 `rgba(91,166,255,.1)` 叠在深色卡片上的结果），而「编辑 / 修改密码」是 `#5399f0` 渐变。故把 `.settings-btn` 基础款直接改成与 `.settings-btn--primary` 同款蓝色渐变 + 白字（仅 padding / 字重略小），demo 同步。
2. **删除提示性文本**。卡片标题下的 `.settings-section__hint` / `.section-heading__hint` 全部删除（账号信息、安全设置、自动刷新、模型解释服务、我的风险阈值、场景启停，以及「上次修改…建议每 90 天更换一次」），AI 编辑弹窗内的 key 说明也删了；demo 里连 `.section-heading__hint` 的 CSS 规则一起清掉。
3. 顺带：`.settings-actions` 补 `gap:10px;margin-top:18px`；本页 `.detail-info` 改为 `gap:0` + 行分隔线（对齐 demo）。

### 验证（可复用）
- `vue-tsc --noEmit -p tsconfig.app.json` → 退出码 0。
- 环境当时已在跑 Vite 5173 + API 12312，**不需要** start_all.ps1。
- Playwright 脚本 `tmp/settings_shot.js`：`#/login` 填 admin/123456 → `#/settings` → 逐个点页签截图 + 断言。
  实测 TABS=`["账号与安全","基础设置","风险阈值","场景管理"]`、HINT_COUNT=0、四页签内容正常、所有 `.settings-btn` 计算样式均为 `linear-gradient(135deg,#5ba6ff,#407acc)` + 白字、改密弹窗可正常打开。
  唯一 console error 是首屏未登录时 `/api/v1/auth/me` 的 401（**既有行为**，与本次改动无关）。
- demo 侧用 `python -m http.server 8791` 起静态服务 + Playwright 复核：同样 0 提示文本、按钮全蓝、无 pageerror。
- 截图产物在 `tmp/shot-settings-*.png`、`tmp/shot-demo-*.png`。
- **角色覆盖**：库里只有 `admin/123456` 能登录（net_admin / alice / bob / carol 全部 401，与种子脚本写的不一致），所以改用 Playwright 改写 `/api/v1/auth/login` + `/auth/me` 响应里的 `role`/`scenario_code` 来模拟另外两级角色（只读、不写库）。脚本 `tmp/settings_roles.js`。
  实测：SCENARIO_ADMIN → 3 个页签（无「场景管理」）；SCENARIO_USER（网络安全 / 电力系统各一）→ 3 个页签，账号信息的角色徽标/绑定场景标签、阈值卡的场景名都正确切换，`editing[thresholdScenario]` 未出现 undefined，三级角色 console/pageerror 均为 0。

### 踩坑
**同一个文件不要在一条消息里并行发多个 Edit**：实测在 `settings-demo/index.html` 上并行 2~3 个 Edit，只有部分生效（工具却都报 success）。逐个串行提交才可靠。

### 遗留（未改，待用户决定）
- 阈值卡片仍显示原始字段名 `network_security`（demo 里也有）—— 属 `bayes-platform-ui-text-cleanup` 的清理范围。
- 风险阈值页签对所有「有绑定场景」的账号可见（沿用正式版 `canConfigureThresholds`，后端 `require_bound_scenario` 也放行场景用户），而 demo 写的是 `only-admin`（场景用户不可见）。此处选择**不改权限、保持现状**。
