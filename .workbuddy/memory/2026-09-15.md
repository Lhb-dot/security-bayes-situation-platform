# 2026-09-15 工作日志

## 1. git 存档
- 提交 `cd52acd work前`：把当时全部改动（含未跟踪的 `homepage-demo/`、`frontend/public/homepage-demo/`、`DashboardBars.vue` 等）先行入库。

## 2. 修复首页 500（服务器内部错误）
- 现象：`/overview` 显示「服务器内部错误 / 重试」，body 为 `code=500`。
- 根因：`backend/app/services/dashboard_service.py` 的 `get_admin_overview` 中变量名不一致 —— 声明 `scenario_cards = []`，追加时写成 `scene_cards.append(...)` → `NameError`。
- 修复：统一为 `scenario_cards`。
- 排查手段：临时脚本绕过 `@service_call` 装饰器（`DashboardService.get_admin_overview.__wrapped__`）直接调用，拿到真实堆栈。

## 3. 首页由「静态 demo 跳转」改为 Vue 组件 + 真实数据库接口（本日主要工作）

### 背景
首页曾被改成 `HomepageDashboardView.vue` 里 `window.location.replace('/homepage-demo/...html')`，
并用 `homepage-runtime.js` 直接改 DOM 里的 KPI 数字 —— 只有 4 个 KPI 是真实数据，其余全是写死的 demo 值。

### 后端：按 `homepage-demo/字段来源说明.md` 重写 `dashboard_service.py`
口径硬约束（全部落地）：
1. 同源衍生去重：carrier 三份（biaoqian/lisan/paired_trail）只计 1 个数据集、取 `biaoqian` 作代表（数值未离散化）；
2. 风险占比按真实标签字段**全量**统计；
3. 风险分固定分箱 0.5-0.7 / 0.7-0.9 / 0.9-1.0；
4. 高置信告警 = `risk_score ≥ 0.8`；
5. ARFF 离散区间 `'(a-b]'` / `'(-inf-b]'` / `'(a-inf)'` 取中点或有限端点；
6. 地质无「区域」字段 → 按真实字段 `Slope` 分坡度档位；
7. 用户端按 `created_by_user_id` 强制过滤本人。

关键实现点：
- `_num()` 必须 `math.isfinite` 过滤：`float('inf')` 会成功解析，区间中点会变成 `inf`，
  进而让 `statistics.pstdev` 抛 `AttributeError: 'float' object has no attribute 'numerator'`（极隐蔽）。
- 新增 `_DatasetReader`：按 dataset.id 缓存 ARFF 读取结果，避免同请求内重复解析。
- 新增 `scenario_key`（network/power/flight_deck/geological）供前端分流；后端原有 `risk_type`
  为 `NETWORK_SECURITY_RISK` 等全称，前端不要直接比对。
- 新增 `DatasetStat.is_risk_label`：`dis_global_catalog` 的 label 是 `landslide_size`（灾害规模），
  不是风险标签，必须排除出「风险占比/先验画像」，否则出现恒 100% 的无意义值。
- 接口：`/dashboard/admin/overview`、`/dashboard/scenarios/{id}/profile`、`/dashboard/scenarios/{id}/workspace`。

实测口径（2026-09-15 真实库）：平台 4 场景 / 9 有效数据集 / 50,345 有效样本；网络 31,453 样本·4,467 异常·14.2%·端口去重 6,870；
电力 2,000·1,594·79.7%；舰面 507·129·25.4%；地质 16,385·4,594·28.0%。与 `字段来源说明.md` 的实测值一致。

### 前端：demo → Vue
- 删除 `frontend/public/homepage-demo/`、`homepage-runtime.js`、`HomepageDashboardView.vue`、`components/home/DashboardBars.vue`。
- 回退 `App.vue`（恢复顶栏 + 导航，首页不再全屏重定向）。
- 新增 `src/components/dashboard/`：`dash.css`（demo 视觉系统，类名全部 `d-` 前缀并挂在 `.dash-page` 下以避免与全局 `.card/.row` 冲突）、
  `dashFormat.ts`、`DashShell/DashKpis/DashCard/DashBars/DashColumns/DashDonut/DashRows/DashFunnel/DashLine/DashGauge/DashEvents/DashScatter/DashRadar`。
- 新增 `src/views/Home/dashboard/`：`DashboardHomeView.vue`（按角色分发）+ `AdminOverviewPage` / `AdminProfilePage` / `UserWorkspacePage`
  + `sections/` 8 个场景分区组件（4 管理端画像 + 4 用户工作台），一一对应 demo 的 9 个页面。
- `dashboardApi.ts` 补全全部契约类型；`scenarioApi.ts` 的 `resolveScenarioId` 增加「纯数字字符串」兼容（路由参数是字符串）。

### 已知数据态（非缺陷）
`risk_event` 表为 0 行 → 用户端 4 个「我的工作台」运行态指标均为 0，并有空态提示。
跑一次「风险研判」并判定为风险后，事件生成即自动有值。

### 本机环境备忘
- 后端：`python -m app.main`，监听 **12312**；前端 Vite dev server 监听 **5173**，`/api` 代理到 12312。
- 后端**没有热重载**，改后端代码必须重启进程。
- 本机种子账号密码已被重置为统一 `123456`（不是 seed 脚本里的 admin123 等）。
- 本机 `bash` 的 PATH 被裁剪，`ls/head/grep` 等不可用 → 临时 `export PATH="/usr/bin:/bin:$PATH"`；
  `PowerShell` 工具 stdout 不回显 → 写文件后用 Read 读；PS 的 `Invoke-WebRequest -SessionVariable` 在非交互模式会报错 → 改用 bash + curl 或 Python 做 HTTP 验证。

## 4. 修复「密码正确但登录失败（502）」
- 现象：登录页输入 admin/123456 → `Request failed with status code 502`。
- **不是密码问题，也不是代码问题**。根因是 **12312 上没有 FastAPI 进程**：
  Vite dev server 把 `/api` 代理到 `127.0.0.1:12312`，上游拒绝连接 → 前端收到 502。
- 成因链：上一轮我用后台任务起的 `python -m app.main` 一直占着 12312 → 用户跑 `start_all.ps1` 时，
  它自己的 FastAPI 抢不到端口启动失败 → 用户取消我的后台任务后，12312 彻底空了。
  另外当时 5173 上并存了**两个** vite 实例（`vite` 与 `vite --host 0.0.0.0`），同样属重复启动。
- 修复：重新拉起 `python -m app.main`，并逐层验证：
  `12312/openapi.json → 200`、`POST 5173/api/v1/auth/login → 200`（浏览器同一路径）、
  直连登录返回 `admin / SUPER_ADMIN`、`GET /dashboard/admin/overview → 200`，
  最后用无头 Chrome 走真实登录表单确认落地「平台运行总览」。
- 教训：**启动进程统一走 `start_all.ps1`**，不要再额外起隐藏的后端实例抢 12312，否则 502 会复现。

## 5. 解耦 demo 目录 + 修正「最外层管理员进入场景」的首页形态
用户三点诉求：URL 不该带 demo 目录、代码不要和 `homepage-demo/` 有关系（该目录后续要删）、
最外层管理员从「场景中心」进入某场景时应看到**场景管理员的首页**而不是平台总览。

### 5.1 与 demo 目录解耦
- 确认 `frontend/public/homepage-demo/` 已删、`dist/` 也无残留；旧地址 `/homepage-demo/**`
  现在只会命中 Vite 的 SPA fallback（返回应用 index.html），静态 demo 已完全不参与运行。
- 把 `homepage-demo/字段来源说明.md` 迁入 **`docs/首页字段口径说明.md`**（去掉对 demo 静态页/路径的引用，
  改为标注真实落地位置：backend service / routes / 前端页面 / 前端契约），避免用户删除 demo 目录后口径文档丢失。
- 14 个前端源码文件 + 后端 `dashboard_service.py` 里所有 `homepage-demo` / `字段来源说明.md` 引用，
  统一改为 `docs/首页字段口径说明.md`。全仓（排除 demo 目录自身）已无残留引用。

### 5.2 首页形态改为「角色 + 是否带场景上下文」双条件
`views/Home/dashboard/DashboardHomeView.vue` 原实现只看角色，导致 SUPER_ADMIN 在
`/scenarios/:scenarioId/dashboard` 也渲染平台总览。改为：
- `SCENARIO_USER` → 我的工作台（任何入口）
- 带 `route.params.scenarioId` → 场景数据画像（SUPER_ADMIN 与 SCENARIO_ADMIN 一致）
- 无场景上下文（`/overview`）→ SUPER_ADMIN 平台运行总览

实测（无头 Chrome 探针 + 截图）：
- 登录后落地 `http://localhost:5173/#/overview`（URL 干净，无 demo 前缀），eyebrow = `Admin · Platform`
- `#/scenarios/geological_risk/dashboard` → h1「地质风险态势 · 数据画像」，eyebrow = `Admin · Data Profile`
- 从「场景中心」点「地质风险」卡片 → 同样进入数据画像 ✓
- 回 `#/overview` → 平台总览 ✓

注：`resolveScenarioId` 之所以兼容数字字符串，是因为 `ScenarioCard`/`ScenarioStatusCard` 传的是
`scenario.scenario_id`，而 `scenarioStore` 把后端 `raw.code` 映射成了 `scenario_id` —— 即路由里实际是**场景编码**。

## 6. 场景中心接入真实数据 + 删除「风险评分」

用户诉求：场景中心 4 张卡片全是 000（数据集/模型/高危事件/风险评分），要接真实数据；并删掉「风险评分：N」这一行。

### 根因
`scenarioStore.mapScenario()`（`fetchScenarioList` 用）把 `dataset_count / model_count / high_risk_count / risk_score`
**全部硬编码为 0** —— 因为 `GET /api/v1/scenarios` 只返回场景元数据，本来就没有统计字段。页面用错了数据源。

### 用户选定的口径
- 数据集：**去重口径**有效数据集数（carrier 同源三份只计 1）
- 模型：**仅已发布**（`status=PUBLISHED`）
- 第三项：**有效样本量**（去重口径样本总数），**不是**「高危事件」

### 后端
- 新增 `DashboardService.get_scenario_overview(current_user)`（`dashboard_service.py`）：
  复用 `_DatasetReader` / `_effective_counts` / `_percent`，口径与 `get_admin_overview` 完全一致；
  已发布模型用**一次 group_by 查询**（避免逐场景 N+1）。
  可见性与 `DatasetService.get_list` 对齐：SUPER_ADMIN 全场景+platform；SCENARIO_ADMIN 绑定场景+platform/company；
  SCENARIO_USER 绑定场景+platform/company+本人 personal。
- 新增路由 `GET /api/v1/scenarios/overview`（`scenario_routes.py`）。
  ⚠️ **必须声明在 `/{scenario_id}` 之前**：FastAPI 按声明顺序匹配，`{scenario_id}` 默认是 `str` 正则，
  `overview` 会先被它吃掉再因 int 解析失败报 422（不是 404，容易误判）。
- 实测（admin）：网络 2/6/31,453、电力 1/0/2,000、舰面 1/0/507、地质 5/0/16,385；totals = 4 场景 / 9 数据集 / 50,345 样本 / 6 已发布模型。耗时 ~1.2s（读 ARFF 标签列，与平台总览同量级）。

### 前端
- `api/scenarioApi.ts`：新增 `getScenarioOverview()` + `ApiScenarioOverview(Card)` 类型。
- `stores/scenarioStore.ts`：新增 `fetchScenarioOverview()` 与 `mapOverviewCard()`。
  注意该数据源下 `Scenario.model_count` **等于已发布模型数**（与 `getGlobalOverview` 的全量版本口径不同），
  已在代码注释里标注；需要区分时读 `published_model_count`。
- `views/Model/ScenarioCenter.vue`：改调 `fetchScenarioOverview()`。
- `components/common/ScenarioCard.vue`：三项指标 → 数据集 / 已发布模型 / 有效样本量（千分位展示）；
  **删除「风险评分：{{ scenario.risk_score }}」整块**及其 `.scenario-card__risk-score`、
  `.scenario-card__stat-value--danger` 死样式。
- 校验：`vue-tsc -b` exit=0；并用 `curl http://127.0.0.1:5173/src/...` 确认 Vite 转译产物已含新逻辑（HMR 已生效）。

### 遗留（未改，待用户决定）
平台运行总览（`AdminOverviewView` / `ScenarioStatusCard`）的 `dataset_count` 仍是前端
`getGlobalOverview()` 按 `datasets.filter(scenario_id===code).length` 统计，**没走去重**：
舰面调度在那里显示 3 个数据集，而场景中心显示 1 个。若要对齐，应把 `getGlobalOverview` 的 `scenarios`
也改读 `/scenarios/overview`（但会改动首页口径文档的落地位置，需先确认）。

## 7. 统一口径：平台总览也改用去重路径（用户选「走去重严谨的道路」）

上条遗留已按用户决定消除 —— **`GET /scenarios/overview` 成为场景卡片的唯一数据源**，
场景中心与平台运行总览共用同一条去重链路，全仓不再有第二套「数据集计数」。

### 后端
- `get_scenario_overview` 补齐两样东西，让平台总览可以完全不再自己算：
  - 每个场景新增 `risk_score` = 该场景风险事件 `risk_score` 均值 ×100（无事件 0）；新增辅助函数 `_risk_score()`。
  - `totals` 扩展为：`scenario_count / effective_dataset_count / effective_sample_count /
    published_model_count / event_count / high_risk_count / risk_score / risk_level`。
- 顺带修掉一个自己造成的 bug：一次 no-op Edit 把 `def _percent(...)` 的函数体挤到了签名同一行，
  已还原并重新格式化。

### 前端
- `scenarioApi.ts`：新增 `toScenarioCard()`（`ApiScenarioOverviewCard → Scenario` 的**唯一**映射），
  场景中心与平台总览共用；`ApiScenarioOverviewCard` 补 `risk_score`，`totals` 补 4 个新字段。
- `situationApi.ts` 的 `getGlobalOverview()`：**重写为薄映射** —— 场景卡片与全局合计全部取自
  `/scenarios/overview`，不再调 `getDatasetList` / `getModelVersionList`（去掉两个大分页请求，
  也去掉了唯一的无去重计数路径）；只保留 `getRiskEventList` 用于 `global_trend` 按日聚合。
  注意 `codeById` 现在从 overview.scenarios 推导，仍用于覆盖 `mapRiskEvent` 的硬编码场景映射。
- `scenarioStore.ts`：新增 `fetchScenarioOverview()` 复用 `toScenarioCard`；给 `mapScenario()` 补注释，
  明确「`GET /scenarios` 无统计字段，统计值恒 0，要真实指标必须用 overview」。

### 校验
- `vue-tsc -b --force` exit=0。
- Vite 转译产物：`getScenarioOverview` 已进三个模块，`getDatasetList` 引用数 **0**。
- 经 Vite 代理端到端（admin）：登录 200 / overview 200 / risk-events 200；
  平台总览将显示 场景数 4、**数据集合计 9（原 11）**、已发布模型 6、样本 50,345、高危 0。

### 文档
`docs/首页字段口径说明.md` 的「⑤ 管理端 · 平台运行总览」已加数据源说明与映射代码位置，
并补上「已发布模型 / 各场景风险分」两行来源与实测值。

---

## 全站品牌名称统一（用户反馈：浏览器标签页显示不全 / 各界面名称不一致）

### 问题
- `frontend/index.html` 与 `frontend/dist/index.html` 的 `<title>` 是残缺占位串 **`ai-`**，
  浏览器标签页就一直显示 "ai-"（用户截图里的箭头所指）。
- 品牌名在各界面不统一：登录页 eyebrow 是 `Bayes Situation Awareness Platform`，
  主界面 eyebrow 是 `AI Security Operations Center`，兜底页面标题又是 `态势感知与威胁可视化平台`。

### 改法（统一为 1 个英文品牌名 + 1 个中文产品名）
- 英文品牌名统一 = **`AI Security Operations Center`**，出现在三处且完全一致：
  `index.html` / `dist/index.html` 的 `<title>`（静态，**不再随路由变化**）、
  `App.vue` 顶栏 eyebrow、`Login.vue` 登录卡 eyebrow。
- `App.vue` 里原先路由驱动的 `documentTitle` + `watch` 已**删除**（连同 `watch` 导入）；
  标签页标题的唯一来源是 `index.html` 的 `<title>`。
- 中文产品名统一 = **`多场景贝叶斯分类态势感知系统`**（与 `Login.vue` 大标题、
  `ReportCenter.vue` 报告落款一致）；`App.vue` 的 `pageTitle` 兜底值由
  `态势感知与威胁可视化平台` 改为它。页面 h1 仍按路由各不相同（这是页面标题，不是品牌名）。

### 尺寸依据（标签页字体下实测，判断是否会被浏览器截断）
`AI Security Operations Center` 在 13px Segoe UI 下宽 **169px**；单个标签可用文本宽约 184px，正常标签栏可完整显示。
备选短名：`AI SOC` 40px、`AI 安全运营中心` 93px、`AI SecOps Center` 100px（用户若要多标签页也不截断可换）。

### 校验（无头 Chrome + CDP，端口 9411/9415）
登录页与 `/overview`、`/scenarios`、`/datasets`、`/risk`、`/reports`、未知路由共 7 个界面：
`document.title` 全部 = `AI Security Operations Center`；`.eyebrow` 全部同一字符串且 `clipped=false`
（`scrollWidth == clientWidth`，顶栏名称没有被省略号裁切）。

## 6. 看板「提示性小字」全站下线（副标题 + KPI 口径副标题 + 卡片来源脚注）
- 需求：截图里标红的 7 处小字全部删除；范围经确认 = **全站所有看板页**（Admin 总览 + 4 个场景画像 + 4 个用户工作台），顶部页面副标题也一并去掉。
- 做法（集中式开关，避免逐个改 9 个分区文件）：新增 `frontend/src/components/dashboard/dashHints.ts` 导出 `SHOW_DASH_HINTS = false`，
  三个共用组件按它门控：
  - `DashShell.vue` → `<p v-if="SHOW_DASH_HINTS && subtitle">`
  - `DashKpis.vue` → `<span v-if="SHOW_DASH_HINTS && item.sub" class="d-kpi-sub">`
  - `DashCard.vue` → 脚注整段包进 `<template v-if="SHOW_DASH_HINTS">`
  - `DashScatter.vue` → 内置 `.d-src` 脚注同样门控
  口径文案仍完整保留在各分区组件的 `sub:` / `source=` 数据里，把开关置 `true` 即可整体恢复，无需改动任何页面文件。
- 刻意未动：`.d-note` 空态引导（只在「无数据」时出现）、图表内坐标轴注释。
- 校验：`vue-tsc --noEmit -p tsconfig.app.json` 通过；Vite dev 各改动模块 transform 全 200 无报错；
  无头 Chrome 登录后实测：Admin 地质画像页与用户工作台页，`.d-top p:not(.d-eyebrow)`=0、`.d-kpi-sub`=0、`.d-src`=0，
  KPI 4 个 / 卡片 7 个正常渲染，无页面错误。

### 本项目 UI 校验配方（可复用，含踩过的坑）
- 前端是 **hash 路由**（`createWebHashHistory`）：`http://localhost:5173/scenarios/xxx/dashboard` 会被当 path 解析，
  必须写成 `http://localhost:5173/#/scenarios/xxx/dashboard`。
- 演示账号密码是 **`123456`**（`docs/数据库配置指南.md` 里的 `admin/admin123` 已过时，实测返回「用户名或密码错误」）。
- 角色 → 首页形态：`admin`(SUPER_ADMIN) + 场景上下文 = 场景数据画像；`carol`(SCENARIO_USER, geological_risk) = 我的工作台。
- Playwright 的 `npx playwright install` 版本与 `%LOCALAPPDATA%\ms-playwright` 缓存（chromium-1234）不匹配会报 executable missing，
  直接 `chromium.launch({ headless: true, channel: 'chrome' })` 借系统 Chrome 最省事。
- 校验脚本放在隔离目录 `C:\Users\11543\.workbuddy-ai\binaries\node\workspace\verify-profile.mjs` / `verify-workspace.mjs`，不污染项目。

## 7. 静态原型页（homepage-demo）同步清掉同类小字
- 范围：`homepage-demo/admin/*.html`（5 页）+ `homepage-demo/user/*.html`（4 页），共 9 页，删掉 140 处：
  `chr-sub`(span/p) / `src` / `foot` / `</h1>` 后紧跟的页面副标题。
- **`homepage-demo/index.html` 未动**：它是静态 demo 的导航入口页，不是看板页，其 `.foot` 是目录结构说明。
- **`homepage-demo/backup-current-5/` 未动**：那是上一版设计的备份快照，改它就失去备份意义。
- 做法：一次性 Python 脚本（二进制读写，保证 CRLF/编码字节级不变），按四个正则删除标记；
  删完后顺手收掉 `.foot` 留下的空行。脚本用完即删，未留在仓库里。
- 各页内联 `<style>` 里的 `.src` / `.chr-sub` / `.foot` 规则**保留**（现在是未使用的死 CSS），
  目的是让恢复只需把标记贴回去，不用重写样式。
- 踩坑：`user/*.html` 的 `.top` 里有两段小字 —— 描述性副标题是无 class 的 `<p>`，
  而范围说明是 `<p class="chr-sub">`（不是 span），第一版脚本只处理 span 漏了 4 处，已补。
- 校验：9 个文件 `class="src|chr-sub|foot"` 计数全为 0；CR 数 == LF 数（行尾一致）；
  无头 Chrome 打开 `admin/geological.html`、`user/network.html`，`.src/.chr-sub/.foot` 均为 0，
  KPI 4 / 卡片 7 正常，无 JS 报错。
- 注：`frontend/dist/` 被 `.gitignore` 忽略、且 start_all 走 `npm run dev`，属未使用的构建产物，本次未重建。

## 8. 行标签里夹带的原始列名清理（用户截图标记）
- 规则：行标签形如「中文译名 + 原始列名」的，只保留译名，删掉列名。
- Vue 端改动：
  - `ProfileFlightdeck.vue`：`平均最小间距 inter_dist_min` / `平均间距 inter_dist_mean` / `中位间距 inter_dist_median`、
    `接近率 |dist_change_ratio| 均值`→`接近率均值`、`相对角均值 relative_angle_mean_deg`、`相对角最大值 relative_angle_max_deg`、
    `间距标准差 inter_dist_std`、`间距极差 inter_dist_range`；并按用户红框去掉 `距离变化率均值（含符号）` 的括注。
  - `ProfileNetwork.vue`：`平均入向字节 AVG IN_BYTES`、`平均重传字节 AVG RETRANSMITTED_IN_BYTES`。
- 静态 demo：`homepage-demo/admin/network.html` 该行原本是裸列名 `AVG IN_BYTES`（连译名都没有），改成译名 `平均入向字节`。
- 判定为**不改**的：`P1/P2`（是飞机编号不是列名）、`T1..T50`（曲线刻度）、`dis_raw_data` 等数据集 id、
  `ftp_data`/`eco_i` 等 KDD 服务**取值**、单位 `Hz`。区分要点：**列名**是字段名，取值/单位/编号不是。
- ⚠️ **并发编辑**：21:37–21:38 有另一个进程（很可能是用户 IDE 里在同时改）动过这批文件 ——
  `ProfileGeological/ProfilePower/WorkspaceNetwork` 的 mtime 也被改了，且它已把 `P1 均值`→`1 号机均值`、
  卡片标题 `接近率（|dist_change_ratio| 均值）`→`接近率均值`、`机间距变化曲线（inter_distance_1..50 全样本均值）`→`（全样本均值）`
  一并清理干净。我的改动与它不冲突，已合并。**下次改这批文件前先看 mtime，避免互相覆盖。**
- 校验：`vue-tsc --noEmit -p tsconfig.app.json` 通过；无头 Chrome 实测 flightdeck / network 画像页，
  `.d-row span` / `.d-kpi-l` / `.d-hbk` 文本已无 snake_case 列名（唯一命中的 `ftp_data` 是 KDD 服务取值，属正常数据）；
  卡片 `title` 也无残留列名。
- 插曲：期间 5173 一度返回 502（Vite 因并发改动重启），稍后自愈。
  **排查坑**：`curl -s` 拿到空响应时 grep 检查会「假通过」，判断服务状态要用 `-w "%{http_code}"`。


