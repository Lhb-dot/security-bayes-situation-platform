# 2026-09-11 工作日志

## 任务：首页统计指标调研 + 5 个首页静态 demo

### 调查结论（数据底座）
- 4 场景 code：`network_security / power_system / flightdeck_operation / geological_risk`（+ Super Admin 平台总览，共 5 个首页）。
- 数据集（`data/`，全量行数/正类）：
  - network: `KDDTrain_20Percent` 7556（anomaly 3522）；`NF-UNSW-NB15-v2` 23897（Label=1 945，正类全在文件后段，首个在第 22952 行）
  - power: `powergrid_knowledgebase_dataset` 2000（Target_Event=1 1594）
  - carrier: `Feature2_Cleaning_biaoqian`/`_lisan`/`paired_TrailData_feature2_biaoqian` 各 507（Collision=1 129）
  - geological: `DIS_raw_data` 5000（Label=1 873）、`DIS_Landslides` 5185（LS=1 2594）、`DIS_Landslide_Causative_Factors` 5000（landslides>0=28）、`DIS_guaruja_random` 200（class=1 99）、`DIS_Global_Landslide_Catalog_Export` 1000（编目）
- 后端已有 `GET /api/v1/scenarios/{id}/insights`（`app/services/scenario_analytics.py`）：按 risk_type 实时算网络/电力/地质/舰面指标；字段名与真实 ARFF 完全对应。**前端从未调用**（grep 零命中）。
- 数据库 PostgreSQL；迁移只有 scenario/algorithm 种子，**无 dataset/model/user 种子**（数据集需界面上传）。模型评估指标 accuracy/f1/recall/precision 由训练写入 `evaluation_metrics`。

### 现状"空谈"根因
1. `frontend/src/api/situationApi.ts` 趋势写死空数组；
2. `NetworkDashboard.vue` 取 `metricValue('model-accuracy')` 当总连接数 → 恒 0；
3. `HomeView.vue` 态势分析 import 自 `@/services/mockApi`（mock）；
4. `GeologicalDashboard.vue` `GEO_RISK_SHARE` 硬编码。

### 无法计算（无源字段，改代码也没用）
网络攻击类型分布、网络目的端口(KDD无该字段)、地理落点地图、电力/网络数据集趋势(无时间戳)、舰面碰撞真实概率(仅二值标签)、地质精确经纬度(仅分箱)与省市分布、管理员系统资源健康度(无监控源)。

### 交付物
`homepage-demo/` 下 5 个静态首页（纯 html+内联 svg，数据来自数据集全量实测，非 mock）：
`network.html / power.html / flightdeck.html / geological.html / admin.html` + `index.html`。
追加：`homepage-demo/字段来源说明.md`（逐字段来源/口径/类型/实测值）。
结论：4 场景页 100% 为数据集字段统计；管理页以数据集统计为主，仅 2 项配置字典 + 4 项运行时指标不来自数据集。

追加（第二套）：`homepage-demo/user/` 下 5 个"用户视角·运行态"静态页（network/power/flightdeck/geological/admin + index）。
字段 = 用户真正关心的运行态指标（待处置/今日新增/高危、告警趋势、我的推理记录、可用已发布模型、等级/状态分布、告警明细）。
数据为**模拟生产数据**（seed=20260911，确定性），口径对齐 risk_events / inference_records / model_versions；合计 592 事件、178 待处置，四场景待处置 81/47/20/30 自洽。

追加（重做）：按用户要求去掉管理页，重做 4 个**各有特色**的场景页（`homepage-demo/user/` 仅 network/power/flightdeck/geological）。
每页不同主线与图表语言：
- 网络=端口攻防（趋势折线+协议环+端口TOP+**端口偏离度**+**处置漏斗**+包长五段）
- 电力=设备健康体检（4 条**电参量刻度条**+7 个**健康度圆环**+问题环+**设备时间线**）
- 舰面=甲板时空（**甲板俯视 SVG 告警分布**+**双机方向角雷达**+间距分布柱+间距折线）
- 地质=易发性（因子贡献条+触发环+**区域易发性排行**+**数据集占比堆叠柱**+触发因素时间线）
合并了现有 4 个 Dashboard 已有且**可实现**的字段（端口偏离度、设备健康度、甲板热点、因子贡献等）。

追加：新增总入口 `homepage-demo/index.html`，一页跳转全部 9 个 demo：
- 管理端 5 个：`homepage-demo/{network,power,flightdeck,geological,admin}.html`（数据画像/平台视角，数值=数据集实测）
- 用户端 4 个：`homepage-demo/user/{network,power,flightdeck,geological}.html`（用户工作台视角，模拟生产数据）

## 实连 PostgreSQL 核对（docker situation-platform-db，5432）
真实行数：scenario 4(全 ACTUAL) / dataset 11(全 platform v1) / model_version 14 / inference_record 3 / **risk_event 0** / app_user 6 / algorithm 6 / risk_threshold 4 / situation_snapshot 0 / handling_record 0 / report 2 / auth_session 79 / threshold_audit_log 20。
- model_version 分布：sc1(网络) DRAFT5+**PUBLISHED3**；sc2 DRAFT1；sc3 DRAFT1；sc4 DRAFT4 → **仅网络有已发布模型**（其他场景用户拿 insights 会 403、也无法推理）。
- inference_record 3 条全属 admin(user1)、model 17、prediction_label 全 "0"、**risk_score 为 NULL**、is_risk_event=false。
- risk_threshold：sc1 0.15/0.17、sc2 0.09/0.16、sc3 0.50/0.80、sc4 0.50/0.80 → **阈值口径不统一且网络/电力过低**。
- 电力各设备 Target_Event=1 占比 77.7~81.8% → 健康度(1-占比)全≈18~22，**无区分度**。
- carrier 三文件均 507 行同批（biaoqian/lisan/paired）→ 记录量重复计数；geo 多文件亦重叠。
- NF-UNSW `FLOW_DURATION_MILLISECONDS` 均值 256471ms 但 **94% 行为 0** → 均值被极值拉偏。

## 口径修正（按用户要求：无意义→有意义）
重做 9 个 demo（管理端5+用户端4）+ 重写 `homepage-demo/字段来源说明.md`。核心替换：
1. 高/中/低危分布(阈值0.15/0.17、0.09/0.16 过低→全高危) → **固定风险分区间 0.5/0.8**
2. 告警趋势(occurred_at=推理时间) → **推理活动趋势**（标注语义）
3. 电力设备健康度(无区分度 18~22) → **设备×问题类型堆叠构成**
4. 网络平均流时长(94%为0) → **重传字节占比**
5. 网络异常率(采样恒0) → **全量异常占比**
6. 平台记录总量(重复计数) → **去重有效样本量 50,345 / 有效数据集 9**
7. 舰面"碰撞概率"(实为分类概率) → **最高模型风险分**
8. 地质"易发性综合指数"(静态) → **分区平均风险分排行**
9. 地质"数据集风险占比" → 保留但标注"静态画像"
机制依据：`occurred_at=inference_record.executed_at`；`risk_score` 仅判风险时写入；`fault_position_x/y` 仅航母场景有值。

## 第二轮检查（假设生产数据存在，结合库/代码）
关键：推理表单 `buildInputData` 按 `fields_schema` 用 sample_value 预填、`validate_input_features` 要求**全部 feature 必填** → `raw_features` 会包含全部场景字段（端口/包长/Component/IssueType/inter_dist 等）→ 用户页聚合**确实算得出、有意义**。
仍存在的问题：
1. `risk_score`=预测类概率，判风险才建事件 → score **恒 ≥0.5**（<0.5 分箱恒 0）；故“风险分≥0.5 告警数”恒等于告警总数（无信息量），应改“≥0.8 高置信”。
2. 舰面甲板坐标是 `_calc_fault_position` 用**双机航向角占位映射**（代码注释“占位”），非真实甲板位置 → 位置语义误导。
3. 地质“分区”字段在真实数据集中**不存在**（DIS_raw_data 无区域列）→ 应按 Lithology/Landuse/坡度档 或 Global Catalog country_name 分组。
4. carrier 模型 accuracy=1.0（507 行）不可信。
5. 用户页后端强制按 `created_by_user_id` 过滤 → 只能叫“我的”，不能叫“场景”。
6. 风险阈值 per-user（risk_threshold PK 含 user_id）→ 跨用户不可比（已用固定分箱规避）。

## 第三轮修正（4 处，已落地到 demo + 文档）
① 网络/电力「风险分≥0.5 告警」→「高置信告警（≥0.8）」（原条件恒成立=告警总数）
② 分数分箱 → 0.5-0.7 / 0.7-0.9 / 0.9-1.0（原 <0.5 档恒空，因事件分数恒 ≥0.5）
③ 舰面「甲板俯视告警分布」→「双机航向映射散点图」（`_calc_fault_position` 是航向角占位映射，非真实甲板坐标），图内标注占位
④ 地质「分区」→「坡度档位」（DIS_raw_data 无区域列，改按真实字段 Slope 分档）
⑤ 用户页统一正名「我的工作台」并标注"仅当前登录用户"
检查脚本结果：9 页禁用词 0 命中、关键新字段全存在、4/4 用户页含范围标注 → 全部通过。
清理：误写到 `D:\001Mine\009Code\project\homepage-demo\`（多一层 dirname 的路径 bug）已删除。

## 打包重组（09-13）
`homepage-demo/` 重组为可打包结构：`index.html`（总入口）+ `admin/`（5 页：network/power/flightdeck/geological/overview）+ `user/`（4 页）+ `字段来源说明.md`（与 index、两文件夹同级）。
- 原 `admin.html` 改名 `admin/overview.html`（避免 admin/admin.html）。
- index 全部相对路径（admin/*.html、user/*.html）；页面本身纯内联零依赖、无绝对路径/外链。
- 文档内路径引用同步更新为 admin/、user/ 前缀。
- 校验：10 个链接全部有效、可移植性 ✅。
