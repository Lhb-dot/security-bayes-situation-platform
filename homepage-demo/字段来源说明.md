# 9 个首页 · 字段来源说明（口径修正版 · 第三轮）

> 对应交付物（打包目录 `homepage-demo/`，全相对路径）：
> - 总入口：`index.html`
> - 管理端 5 个：`admin/`（`network.html`、`power.html`、`flightdeck.html`、`geological.html`、`overview.html`）
> - 用户端 4 个：`user/`（`network.html`、`power.html`、`flightdeck.html`、`geological.html`）
> - 文档（本文件）：`字段来源说明.md`
>
> 本文交代每个字段的**取值来源、口径、类型、实测值**，并单列 **哪些"无意义"字段被替换成了什么**（含三轮核查记录）。

## 一、类型图例

| 类型 | 含义 | 可算性 |
|---|---|---|
| **A · 数据集字段统计** | 对 `data/*.arff` 字段计数/占比/均值/分组 | ✅ 静态可算 |
| **B · 数据集元数据** | 数据集行数、数据集个数 | ✅ 静态可算 |
| **C · 运行时数据库** | 风险事件/推理记录/模型/用户等表聚合 | ✅ 可算，需跑推理才有值 |
| **D · 配置数据字典** | 场景表、算法表 | ✅ 静态可算 |

---

## 二、口径修正总览：无意义字段 → 有意义的替代 ★重点

对照**真实数据库**（PostgreSQL 实查：`risk_event=0`、`inference_record=3`、仅网络有 3 个 PUBLISHED 模型、四场景阈值 0.15/0.17、0.09/0.16、0.50/0.80、0.50/0.80）与**源码机制**（`occurred_at=推理时间`、`risk_score` 仅判风险时写入且恒 ≥0.5、`fault_position` 为航向角占位映射），共替换 15 处：

### 第一轮（机制/口径错误）

| # | 原字段（已剔除） | 为什么无意义（实测依据） | 替换为 |
|---|---|---|---|
| 1 | 网络/电力「高/中/低危 分布」 | 阈值 0.15/0.17、0.09/0.16 过低，`risk_score`>0.5 → 几乎全判高危 | **风险分区间分布**（固定分箱） |
| 2 | 「近 7 天告警趋势」 | `occurred_at`=推理时间，实为用户使用量 | **推理活动趋势**（红=待处置/绿=已处置） |
| 3 | 电力「设备健康度排行」 | 7 类设备故障占比 77.7%~81.8%，健康度全 18~22 → **无区分度** | **设备 × 问题类型 告警构成** |
| 4 | 网络「平均流时长」 | 均值 256,471ms 但 **94% 行为 0**，被极值拉偏 | **重传字节占比** |
| 5 | 网络「异常率（insights）」 | 默认只读前 500 行、正类在后段 → **恒 0** | **异常样本占比（全量）** 14.2% |
| 6 | 平台「记录总量 / 各场景数据量」 | carrier 三文件同批、地质多文件重叠 → **重复计数** | **去重有效样本量** 50,345 / 有效数据集 9 |
| 7 | 舰面「碰撞概率」 | 实为**分类概率**，数据集只有二值标签 | **最高模型风险分** |
| 8 | 地质「易发性综合指数」 | 整份训练集的**静态**归一值 | **坡度档位平均风险分排行** |
| 9 | 地质「各数据集风险占比」放用户页 | 静态先验，与当前告警无关 | 保留但**标注「静态画像」** |
| 10 | 「活跃用户」 | 无定义 | 管理端换真实可查值（账号 6 / 推理记录 3） |

### 第二轮（假设"生产数据已存在"再核查）

先验证：推理表单按 `fields_schema` 以 sample_value 预填、`validate_input_features` 要求**全部 feature 必填** → 事件 `raw_features` 会含全部场景字段，用户页聚合**确实算得出**。但发现 5 处仍需修正：

| # | 问题字段 | 为什么无意义（源码依据） | 替换为 |
|---|---|---|---|
| 11 | 网络/电力「风险分 ≥0.5 告警」 | `risk_score` = 预测类概率，且只在判风险时建事件 → **恒 ≥0.5**，该条件恒成立，等于"告警总数" | **「高置信告警（风险分 ≥0.8）」** |
| 12 | 「风险分区间分布」最低档 `<0.5` | 同上，**恒为空** | 分箱改为 **0.5-0.7 / 0.7-0.9 / 0.9-1.0** |
| 13 | 舰面「甲板俯视告警分布」 | `_calc_fault_position` 用双机航向角映射坐标（代码注释"占位，待与业务方确认"），**非真实甲板坐标** | **「双机航向映射散点图」**（正名 + 图内标注） |
| 14 | 地质「分区平均风险分排行」 | `DIS_raw_data` **无"区域"字段** | **「坡度档位平均风险分排行」**（按真实字段 `Slope` 分档） |
| 15 | 用户页称"场景" | 后端对场景用户强制按 `created_by_user_id` 过滤 → 只有本人事件 | 正名 **「我的工作台」** 并标注"仅当前登录用户" |

> 说明：第 11/12 条是**结构性**问题（分数下限由建事件逻辑决定），不是数据量问题；只要"只在判风险时建事件"，风险分就恒 ≥0.5。

---

## 三、管理端 5 个（数据画像视角 · 数据集全量实测）

### ① 网络安全 `admin/network.html`
数据源：`data/network/NF-UNSW-NB15-v2.arff`(23,897)、`KDDTrain_20Percent.arff`(7,556)｜接口：`/scenarios/1/insights`

| 字段 | 来源方式 | 口径 | 类型 | 实测值 |
|---|---|---|---|---|
| 连接样本总量 | 两数据集行数 | 相加 | B | 31,453 |
| 异常样本数 | `Label=1` + `class=anomaly` | 计数 | A | 4,467 |
| 异常样本占比 | 4,467 ÷ 31,453 | 全量占比 | A | 14.2% |
| 协议分布 / 目的端口 TOP8 | `PROTOCOL` / `L4_DST_PORT` | 分类计数 | A | TCP/UDP/ICMP；53/443… |
| 包长五段 / flag / 服务 TOP | `NUM_PKTS_*_BYTES` / `flag` / `service` | 求和/计数 | A | 见页 |
| **重传字节占比** | `RETRANSMITTED_IN_BYTES ÷ IN_BYTES` | 均值后相除 | A | 见页 |

### ② 电力系统 `admin/power.html`
数据源：`data/power/powergrid_knowledgebase_dataset.arff`(2,000)｜接口：`/scenarios/2/insights`

| 字段 | 来源方式 | 类型 | 实测值 |
|---|---|---|---|
| 监测样本/故障/占比 | `Target_Event=1` | A | 2,000 / 1,594 / 79.7% |
| 电压·电流·温度·频率 均值/范围 | 对应数值列 | A | 见页 |
| 问题类型 / 设备 / 系统分布 | `IssueType` / `Component` / `SystemName` | A | 6 / 7 / 4 类 |
| 平均遥测包丢失 | `Sensor_Packet_Loss_%` | A | 见页 |
| **各设备故障占比（区分度检验）** | `Component × Target_Event` | A | 77.7%~81.8% |

### ③ 舰面调度 `admin/flightdeck.html`
数据源：`data/carrier/Feature2_Cleaning_biaoqian.arff`(507)

| 字段 | 来源方式 | 类型 | 实测值 |
|---|---|---|---|
| 轨迹样本/碰撞样本/占比 | `Collision=1` | A/B | 507 / 129 / 25.4% |
| 最小/平均间距、接近率 | `inter_dist_min/_mean`、`dist_change_ratio` | A | 见页 |
| 双机总航程、方向角统计 | `Plane1/2_total_distance`、`Plane1/2_dir_*` | A | 见页 |
| 间距曲线 / 碰撞 vs 正常 最小间距 | `inter_distance_1..50` / `Collision × inter_dist_min` | A | 见页 |

### ④ 地质风险 `admin/geological.html`
数据源：`data/geological/` 5 个数据集｜接口：`/scenarios/4/insights`

| 字段 | 来源方式 | 类型 | 实测值 |
|---|---|---|---|
| 地质数据集个数 | 文件计数 | B | 5 |
| 主集风险样本/占比 | `DIS_raw_data.Label=1` | A | 873 / 17.5% |
| 8 地形因子均值 | `Slope/TWI/Elevation/Relief/SPI/Dis2roads/Dis2fault/Dis2river` | A | 见页 |
| 各数据集风险占比/样本量 | 各集标签/行数 | A/B | 17.5%/50.0%/0.56%/49.5%；5000/5185/5000/200/1000 |
| 触发因素/类型/规模/国家 | `Global Catalog` 字段 | A | 见页 |

### ⑤ 平台运行总览 `admin/overview.html`

| 字段 | 来源方式 | 类型 | 实测值 |
|---|---|---|---|
| 接入场景 | `scenario` ACTUAL | D | 4 |
| 有效数据集（去重）/ 有效样本量 | 11 文件 − 2 同源衍生 | B | 9 / 50,345 |
| 可用算法 | `algorithm` 表 | D | 6 |
| 各场景有效样本量/数据集数/风险占比 | 按场景聚合 | A/B | 见页 |
| 运行态：风险事件 / 待处置 / 推理记录 / 账号数 | `/situation/global`、`/risk-events`、`/inference-records`、`/users` | C | **0 / 0 / 3 / 6**（真实库值） |

---

## 四、用户端 4 个（运行态 · 模拟生产数据 · 范围=本人）

> 数据为**模拟的生产运行数据**（事件 `raw_features` 驱动，seed 固定）。**范围：仅当前登录用户本人的事件**（后端按 `created_by_user_id` 强制过滤）。

### ① 网络安全 `user/network.html`（我的工作台·端口攻防）
| 字段 | 来源方式 | 类型 |
|---|---|---|
| 我的待处置 / 今日新增 | `risk_event.status / occurred_at` | C |
| **高置信告警（≥0.8）** | `risk_score` 固定 0.8 阈值 | C |
| 异常端口数 / 端口偏离度 | 端口计数 vs 均匀基线 | C |
| 推理活动趋势 | `inference_records.executed_at`（按日） | C |
| 风险分区间分布 | `risk_score` · 0.5-0.7/0.7-0.9/0.9-1.0 | C |
| 告警端口 TOP / 包长五段 / 处置漏斗 / 告警列表 | `raw_features` / `status` | C |

### ② 电力系统 `user/power.html`（我的工作台·设备体检）
| 字段 | 来源方式 | 类型 |
|---|---|---|
| 我的待处置 / 今日新增 / 高置信告警 / 受影响设备 | `risk_event` + `raw_features` | C |
| 告警样本电参量均值（刻度条） | 事件 `raw_features` 电压/电流/温度/频率 | C |
| **设备 × 问题类型 告警构成** | `raw_features.Component × IssueType` | C |
| 设备告警数排行 / 风险分区间分布 / 时间线 | `risk_events` | C |

### ③ 舰面调度 `user/flightdeck.html`（我的工作台·甲板时空）
| 字段 | 来源方式 | 类型 |
|---|---|---|
| 我的待处置 / 今日新增 / 最小间距 / 最高模型风险分 | `risk_event` + `risk_score` | C |
| **双机航向映射散点图**（占位映射） | `fault_position_x/y`（航向角映射） | C |
| 双机方向角雷达 / 最小间距分布 / 推理活动趋势 | 事件 `raw_features` / `executed_at` | C |

### ④ 地质风险 `user/geological.html`（我的工作台·因子分析）
| 字段 | 来源方式 | 类型 |
|---|---|---|
| 我的待处置 / 今日新增 / 高置信告警 / 涉及坡度档位 | `risk_event` | C |
| 关键因子贡献排行 | 事件地形因子均值归一 | C |
| 触发因素分布 | `raw_features.landslide_trigger` | C |
| **坡度档位平均风险分排行 / 坡度档位告警排行** | 事件 `Slope` 分档 × `risk_score` | C |
| 风险分区间分布 / 各数据集先验占比（静态画像）/ 告警列表 | `risk_event` / 数据集标签 | C / A |

---

## 五、附：真实库现状（实连 PostgreSQL 查得）

| 表 | 行数 | 备注 |
|---|---|---|
| scenario | 4 | 全 ACTUAL |
| dataset | 11 | 全 platform、v1 |
| model_version | 14 | **仅网络有 PUBLISHED(3)**；电力/舰面/地质已发布=0 |
| **risk_event** | **0** | 故用户端运行态字段当前为空 |
| inference_record | 3 | 全属 admin、prediction_label=0、risk_score=NULL |
| app_user | 6 | SUPER 1 / 场景管理员 2 / 场景用户 3 |
| algorithm | 6 | — |
| risk_threshold | 4 | 0.15/0.17、0.09/0.16、0.50/0.80、0.50/0.80 |
| situation_snapshot / handling_record | 0 / 0 | 快照与处置记录为空 |
| report / auth_session / threshold_audit_log | 2 / 79 / 20 | — |

---

## 六、第三轮检查结论（逐页）

| 页面 | 结论 | 说明 |
|---|---|---|
| 管理端 网络/电力/舰面/地质 | ✅ 全部有意义 | 数据集实测，不受运行态影响 |
| 管理端 平台总览 | ✅ 全部有意义 | 去重规模 + 真实库值 |
| 用户 网络 | ✅ 全部有意义 | 分数下限问题已修（≥0.8、0.5/0.7/0.9 分箱） |
| 用户 电力 | ✅ 全部有意义 | 设备×问题构成有区分度 |
| 用户 舰面 | ✅ 全部有意义 | 甲板图已正名为航向映射并标注占位 |
| 用户 地质 | ✅ 全部有意义 | 已按真实字段 `Slope` 分档，替代不存在的"分区" |

**仍需注意（非字段问题，属系统配置）：**
1. 需为**电力/舰面/地质**训练并**发布**模型（当前已发布=0），否则场景用户无法推理、也无从产生事件；
2. 网络/电力阈值偏低（0.15/0.17、0.09/0.16）建议统一到 0.5/0.8 一档；
3. 阈值按 `(user_id, scenario_id)` 存储 → 跨用户等级不可比（本方案统一用固定分箱规避）。
