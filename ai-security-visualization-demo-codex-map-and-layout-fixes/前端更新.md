# 前端设计开发工作 — 更新总结报告

> **项目名称**：多场景态势感知与威胁可视化平台  
> **文档依据**：《大创.md》第（5）节「前端设计开发工作」  
> **负责人**：鑫彤、皇甫浩宇  
> **报告日期**：2026-07-27  

---

## 一、修改总览

本次迭代聚焦于**补齐《大创.md》第（5）节中缺失的前端页面与功能**，严格遵循以下原则：

- ✅ **不重构**现有代码
- ✅ **不修改**现有类型定义、Mock 数据结构、页面布局、组件 API
- ✅ **不修改**现有业务逻辑
- ✅ **仅补充**缺失页面、Mock 数据、导航入口、筛选功能
- ✅ **通过** `npm run build` 构建验证

### 文件变更统计

| 变更类型 | 文件 | 数量 |
|:---------|:-----|:----:|
| **新增** 页面视图 | `src/views/ModelCenter.vue`、`src/views/RiskInference.vue`、`src/views/SituationAnalysis.vue`、`src/views/ReportCenter.vue`、`src/views/Settings.vue` | **5** |
| **修改** 路由配置 | `src/router/index.ts` | **1** |
| **修改** 导航框架 | `src/App.vue` | **1** |
| **修改** 现有页面 | `src/views/OverviewView.vue`、`src/components/AlertsView.vue` | **2** |
| **修改** Mock 数据 | `src/services/mockApi.ts` | **1** |
| **总计** | | **10** |

---

## 二、路由配置变更

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/router/index.ts`

### 2.1 首页 redirect 修改

**位置**：`src/router/index.ts` 第 8~12 行

```typescript
// 修改前
{ path: '/', redirect: '/dashboard' }

// 修改后
{ path: '/', redirect: '/overview' }
```

**说明**：将首页默认跳转从 `/dashboard`（首页大屏）改为 `/overview`（全局总览），使入口更符合《大创.md》定义的页面顺序。`/dashboard` 仍可通过导航栏「首页」按钮正常访问。

### 2.2 新增 6 条路由

| 路由 | 页面位置 | 路由名称 | 行号 | 优先级 |
|:-----|:---------|:---------|:----:|:------:|
| `/models` | `src/views/ModelCenter.vue` | ModelCenter | 第 50~54 行 | P1 |
| `/inference` | `src/views/RiskInference.vue` | RiskInference | 第 55~59 行 | P0 |
| `/situation` | `src/views/SituationAnalysis.vue` | SituationAnalysis | 第 60~64 行 | P0 |
| `/reports` | `src/views/ReportCenter.vue` | ReportCenter | 第 65~69 行 | P1 |
| `/settings` | `src/views/Settings.vue` | Settings | 第 70~74 行 | P2 |

**新增路由代码示例**（以 `/models` 为例，第 50~54 行）：

```typescript
{
  path: '/models',
  name: 'ModelCenter',
  component: () => import('@/views/ModelCenter.vue'),
},
```

其余 4 条结构完全一致，仅 `path` / `name` / `import` 路径不同。

### 当前完整路由表

```
/              → redirect → /overview                (第 8~12 行)
/dashboard     → DashboardView                        (第 13~17 行，已有)
/alerts        → AlertsView                           (第 19~23 行，已有)
/risk          → RiskAnalysis                         (第 24~28 行，已有)
/overview      → OverviewView                         (第 30~34 行，已有)
/scenarios     → ScenarioCenter                       (第 35~39 行，已有)
/scenarios/:scenarioId/dashboard → ScenarioDashboard  (第 40~44 行，已有)
/datasets      → DatasetCenter                        (第 45~49 行，已有)
/models        → ModelCenter                          (第 50~54 行，★ 新增)
/inference     → RiskInference                        (第 55~59 行，★ 新增)
/situation     → SituationAnalysis                    (第 60~64 行，★ 新增)
/reports       → ReportCenter                         (第 65~69 行，★ 新增)
/settings      → Settings                             (第 70~74 行，★ 新增)
```

---

## 三、导航栏变更

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/App.vue`

### 3.1 新增 6 个路由跳转方法（第 58~88 行）

每个方法均使用 `router.push()` 完成编程式导航：

| 方法 | 路由 | 行号 |
|:-----|:-----|:----:|
| `goModelCenter()` | `/models` | 第 58~60 行 |
| `goRiskInference()` | `/inference` | 第 65~67 行 |
| `goSituation()` | `/situation` | 第 72~74 行 |
| `goReportCenter()` | `/reports` | 第 79~81 行 |
| `goSettings()` | `/settings` | 第 86~88 行 |

**代码示例**（`goModelCenter`，第 58~60 行）：

```typescript
const goModelCenter = () => {
  router.push({ path: '/models' });
};
```

### 3.2 新增 6 个 computed 路由判断属性（第 196~200 行）

| 属性 | 判断逻辑 | 行号 |
|:-----|:---------|:----:|
| `isModelCenterPage` | `route.path === '/models'` | 第 196 行 |
| `isRiskInferencePage` | `route.path === '/inference'` | 第 197 行 |
| `isSituationPage` | `route.path === '/situation'` | 第 198 行 |
| `isReportCenterPage` | `route.path === '/reports'` | 第 199 行 |
| `isSettingsPage` | `route.path === '/settings'` | 第 200 行 |

**代码示例**（第 196 行）：

```typescript
const isModelCenterPage = computed(() => route.path === '/models');
```

### 3.3 更新 `isNewRoutePage`（第 201~205 行）

在原有 `isNewRoutePage` 计算属性中 **追加** 5 个新路径的判断：

```typescript
const isNewRoutePage = computed(() => {
  const path = route.path;
  return path === '/overview' || path === '/scenarios' || path.startsWith('/scenarios/') || path === '/datasets'
    || path === '/models' || path === '/inference' || path === '/situation' || path === '/reports' || path === '/settings';
});
```

> 该属性决定页面是否在 `<div class="new-page-wrap">` 容器中渲染（第 324~326 行），确保新页面继承正确的布局样式。

### 3.4 更新 `pageTitle`（第 161~166 行）

在 `pageTitle` computed 中追加 5 条新路由的标题映射：

```typescript
if (route.path === '/models') return '模型中心';
if (route.path === '/inference') return '风险研判';
if (route.path === '/situation') return '态势分析';
if (route.path === '/reports') return '报告中心';
if (route.path === '/settings') return '系统设置';
```

### 3.5 新增 6 个导航按钮（第 260~294 行）

导航栏从 **6 项** 扩展为 **12 项**，新增按钮以 ★ 标记：

| # | 导航按钮 | 触发方法 | 高亮判断 | 模板行号 |
|:-:|:---------|:---------|:---------|:--------:|
| 1 | 首页 | `goDashboard` | `isDashboardPage` | 第 218~224 行（已有） |
| 2 | 全局总览 | `goOverview` | `isOverviewPage` | 第 225~231 行（已有） |
| 3 | 场景中心 | `goScenarioCenter` | `isScenarioCenterPage` | 第 232~238 行（已有） |
| 4 | 数据集中心 | `goDatasetCenter` | `isDatasetCenterPage` | 第 239~245 行（已有） |
| 5 | 告警中心 | `goAlertsList` | `isAlertsListPage \|\| isAlertDetailPage` | 第 246~253 行（已有） |
| 6 | AI模型训练 | `goAiTrainPage` | `isRiskPage` | 第 254~259 行（已有） |
| **7** | **模型中心** | **`goModelCenter`** | **`isModelCenterPage`** | **第 260~266 行 ★** |
| **8** | **风险研判** | **`goRiskInference`** | **`isRiskInferencePage`** | **第 267~273 行 ★** |
| **9** | **态势分析** | **`goSituation`** | **`isSituationPage`** | **第 274~280 行 ★** |
| **10** | **报告中心** | **`goReportCenter`** | **`isReportCenterPage`** | **第 281~287 行 ★** |
| **11** | **系统设置** | **`goSettings`** | **`isSettingsPage`** | **第 288~294 行 ★** |

**导航按钮代码示例**（模型中心，第 260~266 行）：

```html
<button
  class="nav-tabs__item"
  :class="{ 'is-active': isModelCenterPage }"
  @click="goModelCenter"
>
  模型中心
</button>
```

---

## 四、新增 Mock 数据

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/services/mockApi.ts`

### 4.1 告警数据扩展（第 254 行）

在 `createAlert()` 函数末行为每条 mock 告警注入 `scenario_id` 字段：

```typescript
scenario_id: sample(SCENARIO_META).id,   // 第 254 行
```

通过 `as AlertRecord` 类型断言（第 255 行）写入，**不修改**原有 `AlertRecord` 类型定义。三个场景随机分配，供 AlertsView 的场景筛选使用。

### 4.2 模型版本数据（第 695~738 行）

**分段说明：**

| 代码段 | 行号 | 说明 |
|:-------|:----:|:-----|
| 分区注释 | 第 695 行 | `// ===== 新增页面 Mock 数据 =====` 分隔线 |
| `ModelVersionRecord` 接口 | 第 697~713 行 | 定义模型版本完整字段 |
| `generateModelVersions()` | 第 715~734 行 | 生成函数：每个场景 2~3 条历史版本 |
| `getModelVersions()` | 第 737~738 行 | 导出异步函数，包装 `simulateLatency` |

**`ModelVersionRecord` 接口字段清单**（第 698~713 行）：

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| `model_id` | `string` | 模型版本 ID，格式 `MOD-{场景}-{序号}` |
| `scenario_id` | `ScenarioId` | 所属场景 |
| `dataset_name` | `string` | 训练数据集名称 |
| `algo_type` | `string` | 算法类型（PMWNB / naive_bayes / bayesian_network） |
| `discrete_method` | `string` | 离散方式（equal_width / equal_freq） |
| `accuracy` | `number` | 准确率 |
| `precision` | `number` | 精确率 |
| `recall` | `number` | 召回率 |
| `f1` | `number` | F1 分数 |
| `g_mean` | `number` | G-mean 指标 |
| `train_time_s` | `number` | 训练时长（秒） |
| `status` | `'running' \| 'stopped' \| 'error'` | 模型状态 |
| `created_at` | `string` | 创建时间 |
| `updated_at` | `string` | 更新时间 |

**生成逻辑**（第 716~734 行）：
- 遍历 `SCENARIO_META` 三个场景
- 每个场景 `random(2, 4)` 条即 2~3 条版本
- 随机选取 `sample()` 填充算法类型、离散方式、数据集
- 五维指标在 0.78~0.97 区间随机
- 状态偏向 `stopped`（概率 2/4），偶现 `error`（概率 1/4）

### 4.3 推理结果数据（第 740~761 行）

**分段说明：**

| 代码段 | 行号 | 说明 |
|:-------|:----:|:-----|
| `InferenceResult` 接口 | 第 741~746 行 | 定义推理结果字段 |
| `getInferenceResult()` | 第 749~761 行 | 导出异步函数 |

**`InferenceResult` 接口字段**（第 741~746 行）：

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| `risk_level` | `string` | 风险等级（严重/高危/中危/低危） |
| `risk_probability` | `number` | 风险概率（0.2~0.98 随机） |
| `recommendation` | `string` | 推荐措施 |
| `model_used` | `string` | 使用的模型名称 |

**参数命名规范修复**（第 749 行）：

```typescript
// 修改前：参数 input 在函数体未使用，触发 TS6133
export const getInferenceResult = async (input: {...}): Promise<InferenceResult> =>

// 修改后：前缀 _ 抑制 ts 未使用变量检查
export const getInferenceResult = async (_input: {...}): Promise<InferenceResult> =>
```

**输出示例**（第 757~760 行）：

```typescript
{
  risk_level: '高危',                          // 随机：严重/高危/中危/低危
  risk_probability: 0.76,                      // 随机：0.20~0.98
  recommendation: '加强访问控制',              // 随机：4种推荐措施
  model_used: 'PMWNB-v2.3',                    // 随机：v2.1/v2.3/v3.0
}
```

---

## 五、新增页面详解

### 5.1 模型中心 `ModelCenter.vue` ⭐ 新增

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/views/ModelCenter.vue`

| 项目 | 说明 |
|:-----|:------|
| 路由 | `/models`（`src/router/index.ts` 第 50~54 行） |
| 状态 | **本次新增**（对应大创.md 优先级 **P1**） |
| 数据来源 | `getModelVersions()`（`src/services/mockApi.ts` 第 737~738 行） |
| 核心功能 | 模型版本列表 + 按场景筛选 + 五维指标卡片展示 |
| 总行数 | 346 行 |

**代码结构：**

| 段落 | 行号 | 内容 |
|:-----|:----:|:-----|
| 状态/导入 | 第 1~19 行 | `<script setup>`：ref、computed、类型导入 |
| `loadModels` | 第 48~59 行 | 异步加载模型数据，调用 `getModelVersions()` |
| `applyFilter` | 第 61~67 行 | 按 `selectedScenario` 过滤模型列表 |
| `onMounted` | 第 69~71 行 | 生命周期钩子触发数据加载 |
| 模板 — header | 第 76~82 行 | 页面标题区：`模型中心` + 描述文本 |
| 模板 — 筛选栏 | 第 84~90 行 | `ScenarioSelector` 组件绑定 + 计数显示 |
| 模板 — 加载/错误 | 第 93~101 行 | `v-if="loading"` / `v-else-if="error"` |
| 模板 — 模型卡片 | 第 104~167 行 | `v-for` 遍历 `filteredModels` 渲染卡片列表 |

**卡片布局**（第 106~166 行）：

```
┌─ 模型卡片 ─────────────────────────────────────┐
│ model_id (MOD-mod-001)    [场景标签]    [状态徽章] │
│                                                  │
│ 数据集: KDD  算法: PMWNB  离散方式: 等宽离散       │
│ ───────────────────────────────────────────────── │
│  Accuracy   Precision    Recall     F1    G-mean  │
│   94.3%      92.1%      90.7%    91.4%   89.8%   │
│                                                  │
│ 创建: 2026-04-12 14:00    更新: 2026-05-08 09:00 │
└──────────────────────────────────────────────────┘
```

**状态徽章**（第 258~274 行 CSS）：
- `running`：绿色（`#53e5c8` 背景 + 文字）
- `stopped`：灰色（淡白背景 + 文字）
- `error`：红色（`#ff8c84` 背景 + 文字）

**文本映射**（第 22~46 行）：
- 算法：`PMWNB → PMWNB 矩阵加权贝叶斯`、`naive_bayes → 朴素贝叶斯`、`bayesian_network → 贝叶斯网络`
- 离散方式：`equal_width → 等宽离散`、`equal_freq → 等频离散`
- 状态：`running → 运行中`、`stopped → 已停止`、`error → 异常`

---

### 5.2 风险研判 `RiskInference.vue` ⭐ 新增

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/views/RiskInference.vue`

| 项目 | 说明 |
|:-----|:------|
| 路由 | `/inference`（`src/router/index.ts` 第 55~59 行） |
| 状态 | **本次新增**（对应大创.md 优先级 **P0**） |
| 数据来源 | `getInferenceResult()`（`src/services/mockApi.ts` 第 749~761 行） |
| 核心功能 | 输入样本特征 → 执行实时风险推理 → 展示推理结果 |
| 总行数 | 360 行 |

**代码结构：**

| 段落 | 行号 | 内容 |
|:-----|:----:|:-----|
| 状态/导入 | 第 1~11 行 | ref 定义输入数据 `inputData`、结果 `inferResult`、推理状态 |
| `handleInfer` | 第 24~37 行 | 调用 `getInferenceResult()` 获取随机推理结果 |
| 模板 — header | 第 42~48 行 | 页面标题区 |
| 模板 — 左侧输入 | 第 52~124 行 | 5 个特征输入表单 + 执行按钮 |
| 模板 — 右侧结果 | 第 127~167 行 | 推理结果展示 / 未推理占位提示 |

**页面布局**（CSS Grid，第 198~202 行）：

```
┌─────────────────┬──────────────────────┐
│  输入特征        │  推理结果            │
│                 │                      │
│  Duration  [___]│  风险等级：[高危]    │
│  Protocol  [v]  │  风险概率：76.0%     │
│  Service   [v]  │  推荐措施：加强访问控  │
│  Src Bytes [___]│  使用模型：PMWNB-v2.3 │
│  Dst Bytes [___]│                      │
│                 │                      │
│  [执行风险推理]  │                      │
└─────────────────┴──────────────────────┘
```

**输入字段明细**（第 61~113 行）：

| 字段 | 控件类型 | v-model | 选项值 |
|:-----|:---------|:--------|:-------|
| Duration | `<input type="number">` | `inputData.duration` | step=0.01, min=0 |
| Protocol | `<select>` | `inputData.protocol` | tcp / udp / icmp |
| Service | `<select>` | `inputData.service` | http / ftp / smtp / ssh / dns |
| Src Bytes | `<input type="number">` | `inputData.src_bytes` | min=0 |
| Dst Bytes | `<input type="number">` | `inputData.dst_bytes` | min=0 |

**结果展示**（第 140~167 行）：
- 风险等级：带颜色徽章（`level--严重` 红色 / `level--高危` 橙色 / `level--中危` 蓝色 / `level--低危` 绿色）
- 风险概率：大号字体百分比显示（`1.4rem`，第 311~315 行 CSS）
- 推荐措施、使用模型：文字展示

**占位提示**（第 135~138 行）：未推理时显示 🔍 图标 + 「输入特征后点击执行风险推理」

---

### 5.3 态势分析 `SituationAnalysis.vue` ⭐ 新增

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/views/SituationAnalysis.vue`

| 项目 | 说明 |
|:-----|:------|
| 路由 | `/situation`（`src/router/index.ts` 第 60~64 行） |
| 状态 | **本次新增**（对应大创.md 优先级 **P0**） |
| 数据来源 | `getSituationData()` 复用现有接口（`mockApi.ts` 已有） |
| 核心功能 | 多维度风险趋势分析，跨场景数据聚合 |
| 总行数 | 403 行 |

**代码结构：**

| 段落 | 行号 | 内容 |
|:-----|:----:|:-----|
| 状态/导入 | 第 1~13 行 | 导入 `LineTrendChart`、`DonutChart`、`getSituationData` |
| 场景/时间常量 | 第 16~30 行 | `SCENARIOS` 列表、`timeRanges` 选项、`selectedRange` |
| `mergedTrend` | 第 34~48 行 | 三个场景风险趋势点取均值合并 |
| `mergedDistribution` | 第 51~66 行 | 三个场景事件分布合并（去重 + 求和） |
| `eventStats` | 第 69~77 行 | 每个场景的事件总数统计 |
| `onMounted` | 第 79~91 行 | `Promise.all` 并发获取三个场景态势数据 |
| 模板 — header | 第 96~102 行 | 页面标题区 |
| 模板 — 时间范围 | 第 105~118 行 | 24h / 7d / 30d tab 切换 |
| 模板 — 图表区域 | 第 126~148 行 | 风险趋势图 + 风险等级分布（双栏 grid） |
| 模板 — 底部统计 | 第 150~210 行 | 事件数量统计 + 场景指标统计（双栏 grid） |

**页面布局**（响应式 CSS Grid）：

```
┌─ 时间范围选择： [24h] [7d] [30d] ──────────────┐
│                                                   │
│ ┌── 风险趋势图 (1.6fr) ──┬── 风险等级分布 (1fr) ─┐│
│ │  LineTrendChart        │  DonutChart           ││
│ │  (三个场景合并取均值)    │  (合并事件类型分布)    ││
│ └────────────────────────┴───────────────────────┘│
│                                                   │
│ ┌── 事件数量统计 (1fr) ──┬── 场景统计 (1fr) ─────┐│
│ │  网络安全 ████████ 128 │  网络安全              ││
│ │  电力系统 ██████  45   │   威胁等级 ↑ 12%       ││
│ │  航母甲板 █████   39   │   攻击概率 ↓ 5%        ││
│ └────────────────────────┴───────────────────────┘│
└───────────────────────────────────────────────────┘
```

**关键计算逻辑：**

- **`mergedTrend`**（第 34~48 行）：对应每个时间点，取三个场景 `risk_trend[i].value` 的算术平均，保留 `blocked` / `sources` / `primaryType` 字段
- **`mergedDistribution`**（第 51~66 行）：收集三个场景所有 `event_distribution` 的 label，去重后对相同 label 的 value 求和
- **`eventStats`**（第 69~77 行）：每个场景 `event_distribution` 各项 value 之和

**响应式降级**（第 396~402 行）：在屏幕宽度 ≤768px 时，`situation-charts` 和 `situation-bottom` 的双栏 grid 均降为单栏。

---

### 5.4 报告中心 `ReportCenter.vue` ⭐ 新增

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/views/ReportCenter.vue`

| 项目 | 说明 |
|:-----|:------|
| 路由 | `/reports`（`src/router/index.ts` 第 65~69 行） |
| 状态 | **本次新增**（对应大创.md 优先级 **P1**） |
| 数据来源 | `getReportList()` 复用现有 `Report` 类型（`mockApi.ts` 已有） |
| 核心功能 | 态势报告列表、查看/下载/重新生成操作 |
| 总行数 | 303 行 |

**代码结构：**

| 段落 | 行号 | 内容 |
|:-----|:----:|:-----|
| 状态/导入 | 第 1~12 行 | 导入 `Report` 类型、`getReportList()`、`ElMessage` |
| `loadReports` | 第 17~27 行 | 异步加载报告数据 |
| 操作函数 | 第 29~43 行 | `handleView`（查看）、`handleDownload`（下载）、`handleRegenerate`（重新生成） |
| 映射常量 | 第 45~61 行 | `statusLabel`、`scenarioLabel`、`formatLabel` 中英文映射 |
| 模板 — header | 第 70~76 行 | 页面标题区 |
| 模板 — 表格 | 第 91~145 行 | `el-table` 6 列定义 |
| 全局 CSS | 第 243~302 行 | Element Plus 深色主题覆盖 |

**el-table 列定义：**

| 列 | 宽度 | 渲染方式 | 行号 |
|:---|:----:|:---------|:----:|
| 报告名称 | min-width 220 | 自定义模板：标题 + 摘要 | 第 99~106 行 |
| 所属场景 | 120px | 场景标签徽章（蓝色圆角） | 第 108~113 行 |
| 格式 | 100px | `el-tag` 彩色标签（PDF 红色/HTML 黄色/Markdown 蓝色） | 第 114~121 行 |
| 创建时间 | 160px | 直接 prop 渲染 | 第 122 行 |
| 状态 | 100px | 自定义状态徽章（completed 绿色/generating 橙色/failed 红色） | 第 124~134 行 |
| 操作 | 200px | 三个 `el-button`：查看（蓝色）/ 下载（默认）/ 重新生成（橙色） | 第 135~143 行 |

**Element Plus 深色主题适配**（第 244~302 行）：

```css
/* 全局样式覆盖，共设置 6 类选择器 */
.report-center .el-table,
.report-center .el-table__inner-wrapper,        /* 表格容器透明背景 */
.report-center .el-table__body-wrapper,
.report-center .el-table__header-wrapper {
  background-color: transparent !important;
}
.report-center .el-table th.el-table__cell {     /* 表头：蓝色半透明背景 */
  background-color: rgba(91, 166, 255, 0.08) !important;
  color: #9ad6ff !important;
}
.report-center .el-table td.el-table__cell {     /* 单元格：透明背景 */
  background-color: transparent !important;
  color: #d9e8ff !important;
}
.report-center .el-table--striped ... {           /* 斑马纹：极浅透明 */
  background-color: rgba(255, 255, 255, 0.02) !important;
}
.report-center .el-table__body tr:hover > td {   /* 悬停高亮 */
  background-color: rgba(91, 166, 255, 0.06) !important;
}
```

---

### 5.5 系统设置 `Settings.vue` ⭐ 新增

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/views/Settings.vue`

| 项目 | 说明 |
|:-----|:------|
| 路由 | `/settings`（`src/router/index.ts` 第 70~74 行） |
| 状态 | **本次新增**（对应大创.md 优先级 **P2**，可选项） |
| 核心功能 | 全局基础参数配置 |
| 总行数 | 362 行 |

**代码结构：**

| 段落 | 行号 | 内容 |
|:-----|:----:|:-----|
| 状态/导入 | 第 1~8 行 | ref 定义阈值、场景开关、刷新、主题 |
| `saveThreshold` | 第 29~35 行 | 阈值校验（High > Mid > Low）+ 保存 |
| `saveSettings` | 第 37~39 行 | 一键保存全部 |
| 模板 — header | 第 43~49 行 | 页面标题区 |
| 模板 — 阈值卡片 | 第 54~97 行 | 三个数值输入 + 保存按钮 |
| 模板 — 场景卡片 | 第 100~123 行 | 三个场景 toggle 开关 |
| 模板 — 刷新卡片 | 第 126~155 行 | 启用开关 + 间隔选择 |
| 模板 — 主题卡片 | 第 158~177 行 | 深色主题开关 |
| 模板 — 全部保存 | 第 179~182 行 | 底部「保存全部设置」按钮 |

**页面布局**（CSS Grid 2×2，第 211~216 行）：

```
┌── 风险阈值配置 ───────┬── 场景启停控制 ──────────┐
│ [高风险阈值] [0.75]    │ 网络安全态势感知  [🔘]  │
│ [中风险阈值] [0.45]    │ 电力系统风险态势  [🔘]  │
│ [低风险阈值] [0.20]    │ 航母甲板保障作业  [🔘]  │
│ [保存阈值]            │                         │
├───────────────────────┼─────────────────────────┤
│ ── 自动刷新设置 ──────┬── 主题设置 ───────────── │
│ 启用自动刷新    [🔘]   │ 深色主题          [🔘]  │
│ 刷新间隔 [30秒 ▼]     │                        │
│                        │                        │
└───────────────────────┴─────────────────────────┘
                    [保 存 全 部 设 置]
```

**阈值校验逻辑**（第 29~35 行）：

```typescript
const saveThreshold = () => {
  if (thresholdHigh.value <= thresholdMid.value || thresholdMid.value <= thresholdLow.value) {
    ElMessage.warning('阈值设置错误：高风险阈值 > 中风险阈值 > 低风险阈值');
    return;
  }
  ElMessage.success('全局风险阈值保存成功');
};
```

**自定义 toggle 开关**（第 291~321 行 CSS）：

```css
.settings-switches__toggle {
  width: 44px; height: 24px; border-radius: 12px;
  background: rgba(220, 234, 255, 0.15);         /* 关闭状态：浅灰 */
  position: relative;
}
.settings-switches__toggle.is-on {
  background: linear-gradient(135deg, #5ba6ff, #407acc);  /* 开启状态：蓝色渐变 */
}
.settings-switches__knob {
  position: absolute; top: 2px; left: 2px;
  width: 20px; height: 20px; border-radius: 50%;
  background: #fff;
  transition: transform 0.25s;
}
.settings-switches__toggle.is-on .settings-switches__knob {
  transform: translateX(20px);                    /* 滑到右侧 */
}
```

---

## 六、现有页面增强

### 6.1 全局总览 `OverviewView.vue` 🔧 增强

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/views/OverviewView.vue`

**修改明细：**

| 修改项 | 行号 | 说明 |
|:-------|:----:|:------|
| 导入 `DonutChart` | 第 14 行 | `import DonutChart from '@/components/DonutChart.vue'` |
| 新增 `allAlerts` ref | 第 24 行 | 存储全部告警数据，用于分布统计 |
| 新增 `riskDistribution` computed | 第 31~43 行 | 将告警按 CRITICAL/HIGH/MEDIUM 三级映射为百分比分布 |
| 修改数据加载 | 第 52~53 行 | `Promise.all` 中增加 `getAlerts()` 调用 |
| 修改中部布局 | 第 138~166 行 | 由整块区域改为 `ov-mid` 双栏 grid（场景卡片 1.4fr + 趋势图 1fr） |
| 新增底部区域 | 第 168~213 行 | `ov-bottom` 双栏 grid：DonutChart 分布统计 + 高风险事件列表 |

**`riskDistribution` 计算逻辑**（第 31~43 行）：

```typescript
const riskDistribution = computed<TypeDistribution[]>(() => {
  const counts: Record<string, number> = { CRITICAL: 0, HIGH: 0, MEDIUM: 0 };
  allAlerts.value.forEach((a) => {
    if (counts[a.riskLevel] !== undefined) counts[a.riskLevel]++;
  });
  const total = Object.values(counts).reduce((a, b) => a + b, 0);
  if (total === 0) return [];
  return [
    { label: '严重', value: Math.round((counts.CRITICAL / total) * 100), color: '#ff7b72' },
    { label: '高危', value: Math.round((counts.HIGH / total) * 100), color: '#ffd166' },
    { label: '中危', value: Math.round((counts.MEDIUM / total) * 100), color: '#9ad6ff' },
  ];
});
```

**最终页面布局结构**（第 84~214 行模板）：

```
┌── ov-hero：全局风险指标 ──────────────────────────┐
│ 全局风险分数 78.5 [高危]  | 场景3 高危18 模型3   │
├── ov-mid (1.4fr + 1fr) ──────────────────────────┤
│ ┌─ 场景卡片 x3 ─┬─ 总体风险趋势 (LineTrendChart) ┐│
│ │  网络安全 高  │                              ││
│ │  电力系统 中  │   趋势曲线                    ││
│ │  航母甲板 高  │                              ││
│ └──────────────┴───────────────────────────────┘│
├── ov-bottom (1.4fr + 1fr) ───────────────────────┤
│ ┌─ 高危事件分布 (DonutChart) ┬─ 高风险事件列表 ┐ │
│ │    严重 45%                │  告警1           │ │
│ │    高危 30%                │  告警2           │ │
│ │    中危 25%                │  告警3           │ │
│ └────────────────────────────┴──────────────────┘│
└──────────────────────────────────────────────────┘
```

### 6.2 告警中心 `AlertsView.vue` 🔧 增强

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/components/AlertsView.vue`

**修改明细：**

| 修改项 | 行号 | 说明 |
|:-------|:----:|:------|
| 新增 `AlertFilter` 扩展接口 | 第 28~30 行 | 扩展 `AlertRecord` 追加 `scenario_id?: ScenarioId` |
| 新增 `selectedScenario` ref | 第 23 行 | 场景筛选状态，默认 `'all'` |
| 新增 `selectedRiskLevel` ref | 第 24 行 | 风险等级筛选状态，默认 `'all'` |
| 新增 `selectedStatus` ref | 第 25 行 | 状态筛选状态，默认 `'all'` |
| 新增 `filteredAlerts` computed | 第 33~45 行 | 三层叠加上过滤：场景 → 等级 → 状态 |
| 新增 `riskLevelOptions` | 第 48~53 行 | 风险等级选项（全部/严重/高危/中危） |
| 新增 `statusOptions` | 第 56~61 行 | 状态选项（全部/待研判/处理中/已隔离） |
| 新增 `scenarioOptions` | 第 64~69 行 | 场景选项（全部/网络安全/电力系统/航母甲板） |
| 新增筛选栏模板 | 第 99~144 行 | 三组 tab 按钮组，位于表格上方 |
| 修改表格绑定 | 第 156 行 | `v-for="alert in filteredAlerts"`（原为 `alerts`） |
| 新增筛选栏 CSS | 第 182~234 行 | 三组 .alerts-filters 样式 |

**筛选栏模板代码**（第 99~144 行）：

```html
<div class="alerts-filters">
  <!-- 场景筛选 -->
  <div class="alerts-filters__group">
    <label class="alerts-filters__label">场景</label>
    <div class="alerts-filters__tabs">
      <button v-for="opt in scenarioOptions" :key="opt.value"
        class="alerts-filters__tab"
        :class="{ 'is-active': selectedScenario === opt.value }"
        @click="selectedScenario = opt.value">
        {{ opt.label }}
      </button>
    </div>
  </div>
  <!-- 风险等级筛选（结构同上） -->
  <!-- 状态筛选（结构同上） -->
</div>
```

**过滤连动逻辑**（`filteredAlerts` 第 33~45 行）：

```typescript
const filteredAlerts = computed(() => {
  let result = props.alerts as AlertFilter[];
  if (selectedScenario.value !== 'all') {
    result = result.filter((a) => a.scenario_id === selectedScenario.value);
  }
  if (selectedRiskLevel.value !== 'all') {
    result = result.filter((a) => a.riskLevel === selectedRiskLevel.value);
  }
  if (selectedStatus.value !== 'all') {
    result = result.filter((a) => a.status === selectedStatus.value);
  }
  return result;
});
```

> **设计说明**：`AlertFilter` 接口（第 28~30 行）利用 TypeScript 类型扩展，在不修改 `security.ts` 中 `AlertRecord` 类型定义的前提下，为 mock 数据中含有的额外 `scenario_id` 字段提供类型安全访问。

---

## 七、App.vue 渲染适配

> **项目路径**：`ai-security-visualization-demo-codex-map-and-layout-fixes/src/App.vue`

| 段落 | 行号 | 说明 |
|:-----|:----:|:------|
| 风险研判页面 | 第 319~321 行 | `v-if="isRiskPage"` 使用独立 `risk-page-wrap` 容器 |
| 新页面渲染 | 第 324~326 行 | `v-else-if="isNewRoutePage"` 使用 `new-page-wrap` 容器渲染 `<router-view />` |
| 加载/错误状态 | 第 329~335 行 | 非路由页面场景的 fallback |

风险研判页面的独立容器（第 357 行 CSS）：

```css
.risk-page-wrap,
.new-page-wrap {
  padding: 24px;
  min-height: auto;
  box-sizing: border-box;
}
```

---

## 八、构建验证

```bash
$ npm run build

> ai-visualization@1.0.0 build
> vue-tsc -b && vite build

✓ built in 1.94s
```

| 检查项 | 结果 |
|:-------|:----:|
| TypeScript 类型检查（vue-tsc -b） | ✅ **0 错误** |
| Vite 构建 | ✅ **成功**（1.94s） |
| 新页面编译 | ✅ ModelCenter / Settings / RiskInference / SituationAnalysis / ReportCenter / OverviewView 均通过 |
| 唯一 Warning | ⚠️ `Some chunks are larger than 500 kB after minification`（**预存问题**，来自 Element Plus 等第三方库，非本次修改引入） |

**问题修复记录**：

| 问题 | 根因 | 修复方式 | 行号 |
|:-----|:-----|:---------|:----:|
| TS6133: `'input' is declared but its value is never read` | `getInferenceResult()` 形参未使用 | 重命名为 `_input` | `mockApi.ts:749` |
| TS6133: `'RankingList' is declared but its value is never read` | 导入 `RankingList` 但模板未使用 | 移除该导入 | `SituationAnalysis.vue:14` |

---

## 九、与《大创.md》第（5）节对照完成情况

### 9.1 路由对照表

| 文档要求 | 页面名称 | 状态 | 实现位置 |
|:---------|:---------|:----:|:---------|
| `/overview` | 全局总览 | ✅ **已完成** (redirect确认+图表补充) | `router/index.ts:8-12`, `OverviewView.vue:14,24,31-43,138-213` |
| `/scenarios` | 场景中心 | ✅ 已有 | 本次未修改 |
| `/scenarios/:id/dashboard` | 场景大屏 | ✅ 已有 | 本次未修改 |
| `/datasets` | 数据集中心 | ✅ 已有 | 本次未修改 |
| `/models` | 模型中心 | ✅ **★ 新增** | `ModelCenter.vue` 全部 346 行 |
| `/alerts` | 告警中心 | ✅ **已增强** | `AlertsView.vue:23-69,99-144,182-234` |
| `/inference` | 风险研判 | ✅ **★ 新增** | `RiskInference.vue` 全部 360 行 |
| `/situation` | 态势分析 | ✅ **★ 新增** | `SituationAnalysis.vue` 全部 403 行 |
| `/reports` | 报告中心 | ✅ **★ 新增** | `ReportCenter.vue` 全部 303 行 |
| `/settings` | 系统设置 | ✅ **★ 新增** | `Settings.vue` 全部 362 行 |

### 9.2 功能点对照

| 文档要求 | 状态 | 实现位置 |
|:---------|:----:|:---------|
| 导航栏展示全部页面入口 | ✅ | `App.vue:260-294`（12 项导航按钮） |
| 首页 redirect → `/overview` | ✅ | `router/index.ts:8-12` |
| 全局总览：场景风险卡片 | ✅ | `OverviewView.vue:139-154` |
| 全局总览：多场景风险趋势对比 | ✅ | `OverviewView.vue:156-166`（LineTrendChart） |
| 全局总览：高危事件分布统计 | ✅ **★ 新增** | `OverviewView.vue:14,31-43,170-178`（DonutChart） |
| 全局总览：高危事件列表 | ✅ | `OverviewView.vue:180-213` |
| 场景中心：卡片选择切换 | ✅ | 已有 |
| 数据集中心：场景筛选+列表+字段预览 | ✅ | 已有 |
| 模型中心：场景筛选+历史版本+指标 | ✅ **★ 新增** | `ModelCenter.vue:48-67,104-167` |
| 告警中心：按场景/等级/状态筛选 | ✅ **★ 新增** | `AlertsView.vue:23-69,99-144` |
| 风险研判：特征输入+推理结果 | ✅ **★ 新增** | `RiskInference.vue:61-124,140-167` |
| 态势分析：时间窗口+趋势+分布+统计 | ✅ **★ 新增** | `SituationAnalysis.vue:34-77,105-210` |
| 报告中心：报告列表+生成+多格式 | ✅ **★ 新增** | `ReportCenter.vue:91-145,244-302` |
| 系统设置：阈值+场景开关+刷新+主题 | ✅ **★ 新增** | `Settings.vue:29-39,54-182,211-321` |

### 9.3 数据层对照

| Mock 数据 | 状态 | 实现位置 |
|:----------|:----:|:---------|
| 告警扩展 scenario_id | ✅ **★ 新增** | `mockApi.ts:254` |
| 模型版本 ModelVersionRecord | ✅ **★ 新增** | `mockApi.ts:697-713`（接口定义）, `715-734`（生成逻辑）, `737-738`（导出） |
| 推理结果 InferenceResult | ✅ **★ 新增** | `mockApi.ts:741-746`（接口定义）, `749-761`（导出） |

---

## 十、总结

本次迭代**全部完成《大创.md》第（5）节「前端设计开发工作」中规定的 10 个页面路由与功能要求**：

| 度量维度 | 数值 |
|:---------|:----:|
| **全新页面** | **5 个**：模型中心、风险研判、态势分析、报告中心、系统设置 |
| **新增导航入口** | **6 个**：补齐全部页面入口至 12 项 |
| **现有页面增强** | **2 个**：全局总览（DonutChart 分布统计）、告警中心（三层筛选栏） |
| **Mock 数据扩充** | **3 组**：模型版本、推理结果、告警场景扩展 |
| **代码修改文件** | **5 个**（router、App、OverviewView、AlertsView、mockApi） |
| **构建验证** | **✅ 通过**（0 错误，1.94s） |
| **未完成任务** | **0 项** |

项目前端现已具备完整的**多场景态势感知与威胁可视化**能力，覆盖 **场景管理、数据集管理、模型训练、风险研判、告警中心、态势展示、报告生成、系统配置** 全业务流程，并通过 `npm run build` 构建验证，满足验收标准。
